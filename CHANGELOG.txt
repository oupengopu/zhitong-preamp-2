v2.1.51 - 2026-07-19
  修复: 代码审核确认的 2/3/4/5/6/7 项问题
    - 固件显示版本号更新为 v2.1.51, 本地网页显示更新为 WEB v2.1.51
    - HA/API `set_input` 服务增加“目标输入等于当前输入”跳过逻辑, 只同步 select/UI, 不再重复执行 `switch_input`, 防止外部控制同一路输入时重复吸合继电器
    - `www/index.html` 的控制请求恢复为隐藏 iframe + form POST 优先, `fetch` 仅作后备; fetch body 改为 `Uint8Array([49])`, 避免空 POST/预检导致 ESPHome 2026.7 web_server_idf 拦截
    - `网页控制/index.html` 改为跳转到 `../www/index.html` 的兼容入口, 不再保留第二套旧控制逻辑, 避免用户打开旧页面时出现按钮状态假切换或控制路径过期
    - 静音/视觉消隐时 MSGEQ7 的 `s_signal_avg` 继续按实时 frame 计算, 并由七段平均值 + 单频段峰值 + 当前输入 MCP gate 联合判定, 避免自动频谱跳转只看平均值导致弱信号抖动
    - 清理 ESPHome 2026.7 编译警告中的可修项: BLE 服务最大音量使用 `int` 避免 `uint8_t > 255` 永假比较, 中文 `snprintf` 缓冲区加大, BLE HID 事件 `switch` 增加 `default`
    - README/CLAUDE/AGENTS 同步更新版本和本次规则; 本次不修改 BLE HID 驱动、不改输出模式防爆音时序、不删任何 ESPHome/PlatformIO 缓存
  原因: 这批问题属于外部控制入口和可维护性修复。网页控制此前容易受浏览器 file:// 跨源/PNA 影响; 旧网页入口又可能让人打开过期逻辑。外部输入服务缺少同输入跳过时, Web/HA 重复选择当前输入仍可能触发继电器动作。静音时频谱视觉可消隐, 但信号判定不能退回平均值单判定, 否则弱信号自动跳转不稳。
  验证:
    - RED 守卫在修改前失败: API set_input 缺少同输入跳过、网页版本不是 v2.1.51、sendRequest 不是 form POST 优先、旧网页不是跳转入口、静音分支 `s_signal_avg` 仍只看平均值、`uint8_t > 255` 和小缓冲区警告仍存在
    - 修改后规则守卫通过: API set_input 同输入跳过、WEB v2.1.51、form POST 优先、`Uint8Array([49])`、旧网页跳转入口、静音信号判定非平均值单判定均匹配
    - `git diff --check` 通过
    - ESPHome 2026.7.0 本地 `config` 通过, 配置展开显示 `firmware_version: v2.1.51`
    - ESPHome 2026.7.0 本地 `compile` 通过, `build_time_str=2026-07-19 23:41:34 +0800`, `config_hash=0xe1068f90`
    - 固件产物生成: `firmware.factory.bin`, `firmware.ota.bin`, `firmware.elf`; `zhitong-preamp-2.bin` 大小 `0x1e1490`, app 分区剩余约 69%
    - 之前点名的可修 warning 已消除; 当前剩余 warning 主要是 `PGA/*.h` 中未使用的备用/调试函数, 本次按 AGENTS 规则不处理
  改动文件:
  智能前级蓝牙2.0.yaml (firmware_version, api set_input, MSGEQ7 静音信号判定, BLE 服务/HID switch/中文缓冲区警告)
  www/index.html
  网页控制/index.html
  CHANGELOG.txt
  AGENTS.md
  README.md
  CLAUDE.md

---
v2.1.50 - 2026-07-19
  修复: 待机呼吸灯历史修复方案丢失, IO16 LED 呼吸仍像未修复
    - 固件显示版本号更新为 v2.1.50
    - 恢复历史修复链 `98fb7b9` / `ef54de4` / `ff0ec2c` 的待机呼吸方案: 待机 1 小时内改为 4s 周期 `sin²` 柔呼吸, 逻辑亮度 `2% -> 65% -> 2%`
    - 待机 1 小时后 5 分钟一次的省电脉冲改为 `sin²` 波形, 避免旧半余弦在边界处观感生硬
    - 移除 IO16 LED 输出的 `last_lvl` / `fabs(lvl-last_lvl)` 阈值滤波, 每 50ms 直接写 PWM, 防止低点和慢变区被跳过
    - 最终 PWM 写入恢复 `lvl * lvl` gamma 近似校正, 对应历史提交 `ff0ec2c breathing: fix float precision, >1h pulse truncation, gamma correction`
    - 本次只改 IO16 面板 LED 的待机呼吸波形和写入方式, 不改 TFT 背光、输入卡片 LED、频谱 LED 点阵、音频防爆音、网页控制或 BLE HID
    - AGENTS/README/CLAUDE 同步更新待机呼吸灯参数, 并在 AGENTS 增加“待机呼吸灯必须保留历史修复波形”规则
  原因: 当前 v2.1.49 代码又回到了旧的 3s/5%~55% 半余弦波形, 并重新出现阈值滤波和线性 PWM 写入。
        项目历史里已经修过这个问题: 需要 `sin²` 波形、无阈值直接写入、gamma 校正三件事同时保留, 否则实机上待机呼吸灯仍会像没有修好。
  验证:
    - RED 守卫在修改前失败: 版本号不是 v2.1.50、缺少 4s `sin²` 呼吸、缺少 >1h `sin²` 脉冲、仍有阈值滤波、缺少 gamma 写入
    - 修改后 YAML 守卫通过: 版本号 v2.1.50、4s `sin²` 呼吸、>1h `sin²` 脉冲、无阈值滤波、`lvl * lvl` gamma 写入均匹配
    - `git diff --check` 通过
    - ESPHome 2026.7.0 本地 `config` 通过, 配置展开显示 `firmware_version: v2.1.50`
    - ESPHome 2026.7.0 本地 `compile` 通过, `build_time_str=2026-07-19 21:37:58 +0800`, `config_hash=0x9b47a374`
    - OTA 上传到 `192.168.31.99` 成功
    - OTA 后复测: 设备 HTTP/web_server 返回 200, API 端口 6053 可连接
    - REST `switch/待机模式/turn_on` 返回 200; `/events` 确认 `待机模式` 为 ON, `显示屏背光` 为 OFF, `静音-HW` 为 OFF
    - IO16 实际呼吸效果需用户肉眼确认; 远程只能确认待机状态已进入、固件分支和 PWM 算法已匹配
  改动文件:
  智能前级蓝牙2.0.yaml (firmware_version, IO16 LED standby breathing interval)
  CHANGELOG.txt
  AGENTS.md
  README.md
  CLAUDE.md

---
v2.1.49 - 2026-07-19
  调整: 硬件静音仅保留在开机保护时序, 日常静音/待机/切换全部改用 PGA2311 软件静音
    - 固件显示版本号更新为 v2.1.49
    - `mute_switch` / MCP23017 GPA6 只在开机流程中先拉低保护, 开机流程结束后释放; 恢复到待机状态时也释放硬件静音
    - `soft_mute_switch.turn_on_action` 不再打开硬件静音, 只先走 `anti_pop_fade_to_silence`, 再固定 `current_db/target_db=-96.0f` 并写 `pga2311::set_volume(0,0)`
    - `soft_mute_switch.turn_off_action` 不再关闭硬件静音, 只在非待机/非保护/非切换时执行 `send_volume_to_pga` 软件恢复音量
    - `enter_standby`、`exit_standby` 不再切 GPA6; 待机进入时只软降/PGA=0, 唤醒时直接由 PGA2311 渐变恢复
    - `switch_input` 和 `switch_output_mode` 保留防爆音软降、PGA=0、继电器后保持窗口, 但不再在运行中打开/关闭硬件静音
    - 温度保护路径也只触发软件静音/待机, 不再额外拉 GPA6
    - AGENTS/README/CLAUDE 同步注明: v2.1.49 起硬件静音只属于开机保护, 以后不要把 `mute_switch` 加回日常运行路径
  原因: 用户确认“静音只有在开机时序是硬件静音, 其他只用软件静音”。直通/变压器切换仍有轻微爆音时, GPA6 硬静音本身也可能引入动作瞬态,
        因此本次不再扩大硬静音时间, 而是把运行期防爆音统一收敛到 PGA2311 软降到 -96dB + PGA=0 + 继电器保持窗口。
  验证:
    - YAML 守卫脚本通过: 非开机段没有 `id(mute_switch)->turn_on()` 或 `switch.turn_on: mute_switch`, 开机段仍保留硬件静音保护
    - `git diff --check` 通过
    - ESPHome 2026.7.0 本地 `config` 通过, 配置展开显示 `firmware_version: v2.1.49`
    - ESPHome 2026.7.0 本地 `compile` 通过, `build_time_str=2026-07-19 19:11:54 +0800`, `config_hash=0x3e1d317d`
    - OTA 上传到 `192.168.31.99` 成功
    - OTA 后复测: 设备 HTTP/web_server 返回 200, API 端口 6053 可连接
    - `/events` 复测设备在线, `静音-HW` 为 OFF, `静音-SW` 为 OFF, `待机模式` 为 OFF, 设备 IP 为 `192.168.31.99`
  改动文件:
  智能前级蓝牙2.0.yaml (firmware_version, boot, soft_mute_switch, enter_standby, exit_standby, switch_input, switch_output_mode, temp_protect)
  CHANGELOG.txt
  AGENTS.md
  README.md
  CLAUDE.md

---
v2.1.48 - 2026-07-19
  修复: 直通/变压器输出模式切换继续残留爆音
    - 固件显示版本号更新为 v2.1.48
    - `switch_output_mode` 保持既有顺序: anti_pop 软降到 -96dB -> PGA=0 -> 硬件静音 -> 切输出继电器 -> 恢复
    - 硬件静音后到切 `relay_out` 的预稳定时间从 30ms 增加到 150ms, 让 GPA6 静音与 PGA=0 先真正稳定
    - 切换 `relay_out` 后立即再次固定 `current_db/target_db=-96dB` 并写 `pga2311::set_volume(0,0)`, 防止继电器动作窗口内音量状态漂回
    - 输出继电器切换后的静音保持时间从 100ms 增加到 450ms, 专门给变压器/直通输出路径的触点弹跳、磁化和直流瞬态留稳定窗口
    - 硬件取消静音后到恢复音量渐变的等待从 20ms 增加到 120ms, 避免刚开声就开始拉升音量
    - 增加 `output_mode` INFO 日志, 每次切输出时记录硬静音和继电器保持阶段, 便于实机听感和日志对应
    - 本次不改输入切换、防爆音统一脚本本体、网页控制、BLE HID、MSGEQ7 频谱或音量公式
  原因: v2.1.23~v2.1.47 已经让输出模式走“软降后硬切”, 所以实机反馈比以前好一点; 但输出模式继电器直接改变后级输出路径, 比输入源继电器更容易产生触点/变压器瞬态。
        当前脚本在硬静音后只等 30ms、切继电器后只等 100ms 就开声, 对变压器/直通路径偏短, 因此继续扩大静音包络而不改其他音频路径。
  验证:
    - 参数守卫脚本先在 v2.1.47 失败, 修改后通过: v2.1.48 版本号、150ms 硬静音预稳定、450ms 输出继电器保持、120ms 开声后等待和 `output_mode` 日志均匹配
    - `git diff --check` 通过
    - ESPHome 2026.7.0 本地 `config` 通过, 配置展开显示 `firmware_version: v2.1.48`
    - ESPHome 2026.7.0 本地 `compile` 通过, `build_time_str=2026-07-19 15:44:55 +0800`, `config_hash=0x203c840d`
    - OTA 上传到 `192.168.31.99` 成功, 设备 HTTP/web_server 重启后返回 200, API 端口 6053 可连接
    - `/events` 复测设备在线, DAC 信号 ON, 背光 ON, `静音-HW/SW` 均 OFF, `SpectrumDiag` OFF
    - 直通/变压器切换听感仍需用户在机身设置页实际切换确认; 输出模式没有单独 Web 实体可远程触发
  改动文件:
  智能前级蓝牙2.0.yaml (firmware_version, switch_output_mode 时序)
  CHANGELOG.txt
  AGENTS.md
  README.md
  CLAUDE.md

---
v2.1.47 - 2026-07-19
  调整: 频谱响应再加快, LED点阵和主页小频谱改用左右声道峰值源
    - 固件显示版本号更新为 v2.1.47
    - `PGA/msgeq7.h` 的 MSGEQ7 上升平滑改为 1.00, 真实瞬态不再先被驱动层慢平均; 下降改为 0.42, 保留少量抗抖但明显快于 v2.1.46
    - 频谱显示层 attack 改为低/中频一帧贴近目标, 高频保留少量软化; gravity 回落速度和峰值衰减同步加快
    - `LED点阵` 和主页小频谱不再使用 `frame.combined` 左右平均值, 改用 `max(frame.left, frame.right)` 后再插值, 避免单声道或左右声道瞬态被平均打平
    - 主页小频谱高度从最高 20px 提到 22px, 但仍只使用显示层数据, 不反馈到自动跳转或输入判定
    - 本次不改网页控制、BLE HID、输入继电器、防爆音、MSGEQ7 raw 门限或自动跳转阈值
  原因: v2.1.46 实机反馈肉眼效果好了很多, 但频谱跳动仍“不真实、跟不上音乐速度”。
        `/events` 播放音乐时 `target/draw` 已经接近, 说明 LVGL 绘制层不是主要滞后点; 剩余问题更可能来自 MSGEQ7 驱动层平滑和 LED 单列视图使用左右平均值。
        因此这次只加快真实频谱进入显示层的路径, 并让单列 LED/mini spectrum 使用更能代表瞬态能量的左右声道峰值。
  验证:
    - 参数守卫脚本通过: v2.1.47 版本号、MSGEQ7 1.00/0.42 平滑、LED/mini 使用 `matrix_src`、gravity 新参数均匹配
    - ESPHome 2026.7.0 本地 `config` 通过, 配置展开显示 `firmware_version: v2.1.47`
    - ESPHome 2026.7.0 本地 `compile` 通过, `build_time_str=2026-07-19 15:30:01 +0800`, `config_hash=0xe211a333`
    - OTA 上传到 `192.168.31.99` 成功, 设备 HTTP/web_server 重启后返回 200
    - `/events` 复测: `style=2` LED点阵, DAC 输入 `mcp_active=1/gate=1`, `raw_peak=21~39` 时 `target/draw` 基本贴合
    - 验证后已关闭 `switch/频谱诊断日志 SpectrumDiag`, 避免正式使用刷 INFO 日志
  改动文件:
  智能前级蓝牙2.0.yaml (firmware_version, 频谱显示 attack/gravity/peak decay, LED点阵与主页小频谱数据源)
  PGA/msgeq7.h (MSGEQ7 上升/下降平滑系数)
  CHANGELOG.txt
  AGENTS.md
  README.md
  CLAUDE.md

---
v2.1.46 - 2026-07-19
  调整: LED点阵改为 audioMotion 风格密集列, 频谱主柱改为快攻 + gravity 回落
    - 固件显示版本号更新为 v2.1.46
    - 参考 `audioMotion-analyzer` 的视觉策略, 只借鉴 LED bars / gravity / peak hold 的设计思想, 不拷贝其 AGPL-3.0 源码
    - `PGA/msgeq7.h` 的 MSGEQ7 上升平滑从 0.35 提到 0.72, 下降从 0.12 提到 0.22, 让低音瞬态更快进入显示帧
    - 频谱显示层从双时间常数慢平滑改为快攻 + 每频段 gravity 回落: 大动态上升可一帧贴近目标, 下降用速度累积保持顺滑
    - `boost_spectrum_value` 的显示专用增益整体再抬一档, 同时降低软膝 knee, 让全屏柱条和主页小频谱都更高一点
    - `LED点阵` 不再使用原来的 7 段左右声道粗 bar, 改用 24 条窄列铺满全屏, 高度按 4px 网格量化, 颜色按强度切换绿/黄/橙/红
    - LED点阵样式下隐藏底部 VU 横条, 底部频率标签改为全宽分布, 更接近参考图的频谱布局
    - 无信号 gate 关闭时同步清空显示值、峰值和 gravity 速度, 避免断信号后旧柱条拖尾
  原因: 实机反馈 v2.1.45 虽然“丝滑”, 但低音出来后柱条要等一阵才明显变化。
        `/events` 诊断也看到 `target` 已变化而 `draw` 仍停在旧高度, 说明主要延迟来自显示层双重平滑。
        这次保留真实 raw 判定和 MCP gate, 只把视觉运动改成更贴音乐节拍的快攻/重力模型。
  验证:
    - 已添加参数守卫脚本并先确认旧参数失败
    - ESPHome 2026.7.0 本地 `config` 通过, 固件版本显示 v2.1.46
    - ESPHome 2026.7.0 本地 `compile` 通过, `build_time_str=2026-07-19 15:04:14 +0800`, `config_hash=0x85bb902d`
    - OTA 上传到 `192.168.31.99` 成功
    - `/events` 确认设备上线、`select/频谱样式 Spectrum=LED点阵`, `switch/频谱诊断日志 SpectrumDiag` 可开关
    - `/events` 无音乐输入时确认 `raw/target/draw` 全 0; 低音同步效果需实机播放音乐后由肉眼确认
  改动文件:
  智能前级蓝牙2.0.yaml (firmware_version, 频谱显示增益, 快攻/gravity 回落, LED点阵 24 列布局, 主页小频谱高度)
  PGA/msgeq7.h (MSGEQ7 上升/下降平滑系数)
  CHANGELOG.txt
  AGENTS.md
  README.md
  CLAUDE.md

---
v2.1.45 - 2026-07-19
  调整: 频谱柱条恢复动态, 蓝表 VU 指针改为细长麦景图风格
    - 固件显示版本号更新为 v2.1.45
    - `boost_spectrum_value` 从 v2.1.43 的强压缩改为低门槛软膝压缩, 实机 `raw_peak=1~15` 的弱信号也能推动柱条; 按实机反馈再整体抬高约一档
    - 6.25k/16k 两段显示门槛降到与弱信号匹配, 提高显示增益, 避免最后两条几乎不跳
    - 频谱显示平滑略加快, 保留顺滑感但让高频瞬态能跟随音乐起落
    - 主页中间小频谱改用同一套真实频谱显示值, 并把高度从 4~16px 提到 4~18px, 透明度随频谱强度变化
    - 蓝表 VU 指针从 10 个粗点改为每声道 40 个 2x1 密集细段 + 6px 底部轴心, 观感接近麦景图表头的一根细长黑针
    - 蓝表 VU 电平从旧的 `avg*2+max` 降为 `avg*60%+peak*40%`, 避免指针长期顶在高位, 恢复低/中/高起落
    - 继续不依赖 `LV_USE_LINE`/`LV_USE_CANVAS`, 因当前 ESPHome LVGL 配置二者均为 0
    - 新增 `switch/频谱诊断日志 SpectrumDiag`, 默认关闭; 测试时每秒输出 raw/target/draw 7 段数据, 并附带 `mcp_active/gate/idle_cal` 与 ADC diff/offset
    - 当前输入 MCP 检测无信号时, 频谱显示 gate 直接归零, 且所有输入无信号稳定 3 秒后自动触发 MSGEQ7 静默重校准
    - `msgeq7::recalibrate()` 更新 offset 后同步清空平滑值、峰值和最后 raw 快照, 避免旧底噪残留继续显示
    - 自动跳转信号判定从只看七段平均值改为“七段平均 + 单频段峰值”联合值, 并要求当前输入 MCP 检测有效
    - 自动跳转弱信号保持窗口从 1.5 秒放宽到 5 秒, 避免音乐间隙或只有少数频段跳动时反复清零 15 秒稳定计时
  原因: 实机照片显示 v2.1.43 把频谱视觉压得过低, 尤其最后两段失去动态。
        蓝表 VU 的粗点阵斜线不像真实模拟功率表, 需要向麦景图蓝表头的细长指针靠拢。
        同时关信号后频谱仍会跳动, `/events` 验证 MCP 检测全 OFF 但旧 offset 下 `raw_peak` 可到 20~69,
        说明不能继续靠压低显示层解决, 必须在无信号时重新采静默基线; 重新校准后开信号实测 `raw_peak` 多数只有 1~15,
        因此有信号状态下显示门槛必须按弱信号幅度重新拉低。之后又验证到 MCP 已 ON 时 `raw_avg` 仍常为 0/1,
        但 `raw_peak` 有 4~6 的单频段跳动, 旧自动跳转只看平均值会导致稳定计时反复清零。
  验证:
    - ESPHome 2026.7.0 本地 `config` 通过
    - ESPHome 2026.7.0 本地 `compile` 通过, 生成 `firmware.ota.bin`
    - OTA 上传到 `192.168.31.99` 成功
    - `/events` 关信号验证: `mcp_active=0 mcp_any=0 gate=0 idle_cal=1`, `target/draw` 全 0
    - `/events` 开信号验证: `mcp_active=1 mcp_any=1 gate=1`, 实测 `raw_peak` 多数 1~15, 因此二次调低显示门槛并提高增益
    - 二次 OTA 后开信号复测: `raw_peak=12~42` 时 `target/draw` 约 80~200, 6.25k/16k 不再长期为 0
    - 二次 OTA 后关信号复测: `mcp_active=0 mcp_any=0 gate=0 idle_cal=1`, `raw/target/draw` 全 0
    - 待三次 OTA 后验证: 有信号且输入检测 ON 时 `sig>=2` 能稳定保持并自动跳到频谱页
  改动文件:
  智能前级蓝牙2.0.yaml (firmware_version, 频谱显示映射, 高频响应, 主页小频谱, 蓝表 VU 细长指针和电平权重, 频谱诊断日志, 无信号 gate/静默重校准, 自动跳转弱信号判定)
  PGA/msgeq7.h (recalibrate 清空平滑/峰值/last_raw)
  CHANGELOG.txt
  AGENTS.md
  README.md
  CLAUDE.md

---
v2.1.44 - 2026-07-19
  调整: 频谱样式暴露到 Web/HA, 便于确认蓝表 VU 指针
    - 固件显示版本号更新为 v2.1.44
    - 新增 `select/频谱样式 Spectrum`, 选项与机身设置页一致: 经典柱状/镜像频谱/LED点阵/示波线/蓝表VU/星点火线
    - 开机恢复状态时发布当前 `spectrum_theme`, 让 `/events` 能看到当前频谱样式
    - 机身设置页旋钮切换频谱样式时同步发布到 `spectrum_style_select`
    - 远程选择“蓝表VU”时写入 `spectrum_theme` 并刷新设置页, 用于确认蓝表头点段式指针是否显示
    - 本次不改频谱软膝映射、不改高频压制、不改输入卡片 LED
  原因: 蓝表头指针只在 `spectrum_theme == 4` 时显示, 但此前该状态没有导出到 Web/HA。
        远程看不到当前是否处于“蓝表VU”, 容易把“没有切到蓝表样式”误判成“指针消失”。
  验证:
    - ESPHome 2026.7.0 本地 `config` 通过
    - ESPHome 2026.7.0 本地 `compile` 通过, 生成 `firmware.ota.bin`
    - OTA 上传到 `192.168.31.99` 成功
    - `/events` 确认 `select/频谱样式 Spectrum` 可见, 并已远程切到“蓝表VU”
  改动文件:
  智能前级蓝牙2.0.yaml (firmware_version, spectrum_style_select, 频谱样式状态发布)
  CHANGELOG.txt
  AGENTS.md
  README.md
  CLAUDE.md

---
v2.1.43 - 2026-07-19
  调整: 频谱显示映射改为软膝压缩, 避免低中频/高频残留顶满
    - 固件显示版本号更新为 v2.1.43
    - `boost_spectrum_value` 从线性大倍率放大改为 `energy * 255 / (energy + knee)` 软膝压缩
    - 低/中频显示倍率下调, 弱信号仍可见但不再轻易满格, 频谱分布更接近真实音乐频段
    - 6.25k/16k 显示底噪继续提高并降低倍率, 历史高频残留值会归零或低位显示, 强高频内容仍可见
    - 继续保留 v2.1.42 的时间常数平滑、峰值快速衰减、火花样式去抖
    - 蓝表头点段式指针和输入卡片 LED 逻辑不改
  原因: 数值审计发现 v2.1.42 虽然压住 16k, 但低/中频线性倍率仍过高, 典型弱信号也会被放到很高甚至满格。
        这会让整体看起来仍不像自然频谱。软膝压缩能保留弱信号可见度, 同时避免单个频段长期顶住画面。
  验证:
    - ESPHome 2026.7.0 本地 `config` 通过
    - ESPHome 2026.7.0 本地 `compile` 通过, 生成 `firmware.ota.bin`
    - OTA 上传到 `192.168.31.99` 成功; 重启后 Web 事件流可读, DAC 信号 ON, 背光 ON, 非静音, 非待机
  改动文件:
  智能前级蓝牙2.0.yaml (firmware_version, 频谱显示软膝压缩, 高频显示压制)
  CHANGELOG.txt
  AGENTS.md
  README.md
  CLAUDE.md

---
v2.1.42 - 2026-07-19
  调整: 频谱显示继续变丝滑并进一步压低高频柱底噪
    - 固件显示版本号更新为 v2.1.42
    - 频谱显示层从固定快起慢落系数改为按 `display_dt` 计算的时间常数平滑, 低频响应更快, 高频响应更慢, 跳动更连贯
    - 6.25k/16k 显示侧 `visual_floor` 继续提高, `visual_gain_q8` 继续降低, 避免最后两根柱被高频残留长期顶高
    - 峰值线衰减从每秒保留 42% 改为 30%, 减少高频 peak 长时间挂在高处的错觉
    - 火花/流光频谱样式移除人为 `flicker` 抖动, 柱高只跟真实七段频谱插值走
    - 输入卡片 LED 保持 v2.1.39 MCP23017 状态灯逻辑, 本次不改
  原因: 实机反馈 v2.1.40/v2.1.41 后频谱仍“不丝滑”, 且 6.25k/16k 仍偏高。
        这些现象属于显示映射问题, 继续在 UI 绘制层调 `visual_floor/visual_gain_q8` 和显示平滑, 不碰 MSGEQ7 raw 采样和自动信号判定。
  验证:
    - ESPHome 2026.7.0 本地 `config` 通过
    - ESPHome 2026.7.0 本地 `compile` 通过, 生成 `firmware.ota.bin`
    - OTA 上传到 `192.168.31.99` 成功; 重启后 Web 事件流可读, DAC 信号 ON, 背光 ON, 非静音, 非待机
  改动文件:
  智能前级蓝牙2.0.yaml (firmware_version, 频谱显示平滑, 高频显示压制, 火花样式去抖)
  CHANGELOG.txt
  AGENTS.md
  README.md
  CLAUDE.md

---
v2.1.41 - 2026-07-19
  修复: 蓝表头指针可见性再加固
    - 固件显示版本号更新为 v2.1.41
    - `LV_USE_LINE=0`, 不能依赖 LVGL line 控件; 旋转 78x3 obj 指针也可能受变换方向/裁剪影响
    - 蓝表头指针改为 10 个小 `obj` 点段拼成一根指针, 每帧直接按 225°~315° 表盘弧线计算 x/y 坐标
    - 旧 `vu_needle_l/r` 旋转对象保留但进入蓝表头模式时强制隐藏, 避免和新点段指针冲突
    - 频谱平滑、高频压制和输入 LED 均保持 v2.1.40/v2.1.39 逻辑不变
  原因: v2.1.40 的旋转 obj 指针编译和 OTA 都正常, 但若 LVGL 对对象 transform 的正负方向或外扩绘制区域与预期不同,
        实机仍可能看不到指针。点段式指针完全绕过 transform/line 依赖, 只用已验证可用的 `obj + x/y + bg_opa`。
  验证:
    - ESPHome 2026.7.0 本地 `config` 通过
    - ESPHome 2026.7.0 本地 `compile` 通过, 生成 `firmware.ota.bin`
    - OTA 上传到 `192.168.31.99` 成功; 重启后 Web 事件流可读, DAC 信号 ON, 背光 ON, 非静音, 非待机
  改动文件:
  智能前级蓝牙2.0.yaml (firmware_version, 蓝表头点段式 VU 指针)
  CHANGELOG.txt
  AGENTS.md
  README.md
  CLAUDE.md

---
v2.1.40 - 2026-07-19
  修复: 频谱跳动不丝滑、高频柱长期偏高、蓝表头指针不可见
    - 固件显示版本号更新为 v2.1.40
    - 频谱显示层新增独立快起慢落平滑: 每帧先算目标高度, 再用上升快/下降慢的显示值驱动 LVGL bar
    - 峰值线改用显示层峰值保持和按时间衰减, 不再直接使用放大后的 raw peak, 让频谱跳动更连贯
    - 16kHz 频段显示底噪扣除从 36 起算并降低倍率到 1x, 6.25kHz 也降低显示倍率, 避免高频柱长期压住画面
    - 蓝表头指针恢复为表盘弧线角度 220°~320°、加粗到 3px, 进入蓝表头样式时强制设置位置/颜色/pivot, 避免负角度/水平摆法导致指针消失
    - 输入 LED 不改: 保持 v2.1.39 的 MCP23017 有信号常亮、无信号熄灭逻辑
  原因: v2.1.39 只做了音量联动和显示均衡, bar 仍直接跟随每帧整数值, 视觉会生硬。
        实机反馈 16kHz 柱仍长期偏高, 说明显示侧高频底噪还要继续扣除和降倍率。
        旧蓝表头指针用 -42°~+42° 水平线摆动, 高位会向表盘下方转, 并且负角度/2px 细线在 LVGL 上不稳定。
  验证:
    - ESPHome 2026.7.0 本地 `config` 通过
    - ESPHome 2026.7.0 本地 `compile` 通过, 生成 `firmware.ota.bin`
    - OTA 上传到 `192.168.31.99` 成功
    - OTA 后 `/events` 确认 DAC=ON, 背光=ON, 静音=OFF, 待机=OFF, 音量=52.2%, 设备重启后在线
  改动文件:
  智能前级蓝牙2.0.yaml (firmware_version, 频谱显示平滑, 高频显示压制, 蓝表头指针)
  CHANGELOG.txt
  AGENTS.md
  README.md
  CLAUDE.md

---
v2.1.39 - 2026-07-19
  调整: 频谱/蓝表头/输入信号灯视觉表现
    - 固件显示版本号更新为 v2.1.39
    - 频谱柱显示增益改为“频段视觉均衡 + 当前音量联动”, 音量越大显示幅度越大, 音量为 0 时显示不再固定跳动
    - 低/中频显示倍率继续加大, 6.25k/16k 保留显示底噪扣除, 避免高频底噪压住其他频段
    - 蓝表头 VU 指针从单纯 7 段平均值改为“平均值 + 峰值”驱动, 并使用快起慢落的指针惯性
    - 输入选择卡片 LED 改为 MCP23017 `audio_cd/dac/pc/aux` 稳定状态灯: 有信号常亮, 无信号熄灭, 不再随 MSGEQ7 强度呼吸闪动
  原因: v2.1.38 已确认七段频率顺序和显示均衡基本正确, 但 MSGEQ7 采样位置不随 PGA2311 音量变化,
        UI 若只按 raw frame 绘制, 实机会表现为柱子太低且不随音量变大。蓝表头只用平均值会把节奏峰值抹平,
        输入 LED 用 `s_signal_avg` 呼吸也不符合“输入有无信号状态灯”的语义。
  验证:
    - ESPHome 2026.7.0 本地 `config` 通过
    - ESPHome 2026.7.0 本地 `compile` 通过, 生成 `firmware.ota.bin`
    - OTA 上传到 `192.168.31.99` 成功
    - OTA 后 `/events` 确认 DAC=ON, 背光=ON, 音量=48.2%, 设备重启后在线
  改动文件:
  智能前级蓝牙2.0.yaml (firmware_version, 频谱显示音量联动, 蓝表头 VU, 输入信号 LED)
  CHANGELOG.txt
  AGENTS.md
  README.md
  CLAUDE.md

---
v2.1.38 - 2026-07-19
  修复: 频谱已经可见但不像按频率分段跳动
    - 固件显示版本号更新为 v2.1.38
    - 临时 OTA 诊断 `msgeq7_bands` 验证 7 段确实分别读数, 不是 STROBE 顺序错, 也不是 UI 把 7 段混成平均
    - 实机 DAC 日志显示第 7 段/16kHz raw diff 长期偏高, 例如 `diff=14/13/12/11/11/16/25`, frame 第 7 段常到 `40~65`
    - 将频谱显示从统一 `v*4 + min 10` 改为按频段视觉均衡: 低/中频提高可见度, 6.25k/16k 先扣显示底噪再降低显示倍率
    - `msgeq7_bands` 明细日志保留为 DEBUG 级, 正式 INFO 运行不刷屏
  原因: v2.1.37 的统一显示增益会把高频底噪/残留继续放大, 16k 段长期接近满格; 其他小信号又被最低可见值拉到相近高度,
        所以实机视觉上不像按 63Hz/160Hz/400Hz/1k/2.5k/6.25k/16k 分段跳动。
  验证:
    - ESPHome 2026.7.0 本地 `compile` 通过
    - OTA 上传到 `192.168.31.99` 成功
    - OTA 后 `/events` 确认 DAC=ON, 静音=OFF, 待机=OFF, 背光=ON
    - 正式 INFO 日志没有 `msgeq7_bands` 刷屏
  改动文件:
  智能前级蓝牙2.0.yaml (firmware_version, 频谱显示视觉均衡, msgeq7_bands DEBUG 明细日志)
  CHANGELOG.txt
  AGENTS.md
  README.md
  CLAUDE.md

---
v2.1.37 - 2026-07-19
  修复: MSGEQ7 已有弱信号数据但实机频谱柱仍几乎看不见
    - 固件显示版本号更新为 v2.1.37
    - 频谱 UI 在绘制前增加显示专用增益: 非零值按 4 倍放大, 最低可见值为 10, 最高限制 255
    - 信号检测仍使用未放大的 raw frame 平均值, 不把显示增益反馈到自动跳转/LED 判定
    - `s_signal_avg` 改为快速上升、慢速回落, 避免弱信号短暂掉底后立即清零
    - 频谱自动跳转阈值从 `s_signal_avg > 2` 改为 `s_signal_avg >= 2`, 并增加 1.5 秒弱信号保持窗口
  原因: v2.1.36 实机诊断已确认 DAC 输入下 `frame_peak` 可到 9~17, `signal` 可到 4~9, 说明 MSGEQ7 驱动已经不再全 0。
        但 9~17 直接映射到 78/88px 高的 LVGL bar 时只有几像素, 看起来仍像没有频谱; 同时弱信号偶尔等于 2 时, 旧 `>2`
        判断会重置自动跳转计时。
  验证:
    - ESPHome 2026.7.0 本地 `config` 通过
    - ESPHome 2026.7.0 本地 `compile` 通过, 生成 `firmware.ota.bin`
    - OTA 上传到 `192.168.31.99` 成功
    - OTA 后 `/events` 确认 DAC=ON, 静音=OFF, 待机=OFF, 背光=ON, 音量约 44%
  改动文件:
  智能前级蓝牙2.0.yaml (firmware_version, 频谱显示增益, 弱信号检测抗抖)
  CHANGELOG.txt
  AGENTS.md
  README.md
  CLAUDE.md

---
v2.1.36 - 2026-07-19
  修复: MSGEQ7 弱信号无显示 + 本地网页按钮提示已发送但实机仍无动作
    - `PGA/msgeq7.h` 新增 `RAW_NOISE_FLOOR=6`, 在 raw-offset 后先扣除 ADC 底噪
    - 新增 `SIGNAL_FULL_SCALE_RAW=128`, 用实机有效包络窗口映射到 0-255, 不再按 ADC 4095 全量程缩放
    - 保留频段增益先于缩放, `NOISE_GATE=2` 作为最后的显示门限
    - 固件显示版本号更新为 v2.1.36
    - `www/index.html` 控制请求改为优先 `fetch` CORS POST, 成功时必须拿到 HTTP 200, 失败时显示实体路径错误
    - CORS/PNA 被浏览器拦截时再降级为 `no-cors` POST, 最后才使用隐藏 form 兜底
    - 网页版本更新到 `WEB v2.1.36`; 浏览器保存的旧 IP 会在网页版本变化时重置到当前默认 `192.168.31.99`
    - 支持用 `?ip=192.168.x.x` 临时指定设备 IP, 并写回本地保存值
    - 验证通过后将 `anti_pop` 与 `msgeq7_diag` 临时 INFO 日志恢复为 DEBUG, 最终固件保持 INFO 运行不刷屏
  原因: MSGEQ7 临时诊断实测 DAC 输入检测 `mcp=0/1/0/0`, raw 在 offset 附近有约 10~15 count 变化, 但旧算法按 4095 全量程缩放后低于 `NOISE_GATE`, 导致 `frame_peak=0 signal=0`。网页侧 curl 直测 ESPHome REST 接口正常, 但旧版总是盲目使用隐藏 form 并立即提示"已发送", 浏览器拦截或 localStorage 残留旧 IP 时没有任何可见错误。
  验证:
    - OTA 临时诊断版后, DAC 输入下 `frame_peak` 从 0 变为 9~17, `signal` 变为 4~9
    - 静音开启日志显示先淡到 `current=-95.50` 后才 `静音-HW >> ON`
    - 输入 DAC/CD 切换日志显示先淡到约 `current=-95.5` 后才硬静音、切输入、再解除硬静音
    - 本地 HTTP 打开网页 `WEB v2.1.36`, 连续两次播放控制无前端错误; 设备 `BLE HID 按键事件` 递增到 `PLAY#6`
  改动文件:
  PGA/msgeq7.h (raw域底噪扣除, 有效窗口缩放)
  智能前级蓝牙2.0.yaml (firmware_version, 诊断日志恢复 DEBUG)
  www/index.html (POST 发送层, IP 初始化, 网页版本)
  CHANGELOG.txt
  AGENTS.md
  README.md
  CLAUDE.md

---
v2.1.34 - 2026-07-19
  修复: MSGEQ7 弱信号已放大但频谱自动判定仍按旧阈值认为无信号
    - 频谱自动跳转的 `has_signal` 阈值从 `s_signal_avg > 8` 改为 `s_signal_avg > 2`
    - 注释同步为跟随当前 `NOISE_GATE=2`, 避免以后只改驱动门限却忘记自动判定门限
    - 固件显示版本号更新为 v2.1.34
  原因: v2.1.33 已把 MSGEQ7 驱动侧 `NOISE_GATE` 降为 `2`, 但自动频谱页仍保留旧的 `>8` 判断和旧注释。弱信号即使进入 `s_signal_avg`, 也可能达不到旧阈值, 表现为“MSGEQ7 有输入但页面/自动跳转仍像没信号”。
  改动文件:
  智能前级蓝牙2.0.yaml (firmware_version, has_signal 阈值)
  CHANGELOG.txt
  AGENTS.md
  README.md
  CLAUDE.md

---
v2.1.33 - 2026-07-19
  修复: 本地网页控制、媒体按钮重复触发、防爆音目标抢占、MSGEQ7 弱信号门限
    - `www/index.html` 所有控制命令默认改为隐藏 iframe + form POST, 不再先走 `fetch/no-cors`
    - `file://` 和本地 HTTP 打开网页时都使用 form POST, 避免 Chrome/Edge 对私有网络 fetch 的静默拦截或假成功
    - SSE 未连接/重连中时不再用全屏遮罩拦住控制区, 只降低透明度提示状态
    - www/index.html 的 `mapEntity()` 改为合并实体信息, `/events` 后续精简状态包缺少 `name/domain` 时不再覆盖已知实体名称
    - 网页新增媒体按钮实体兜底映射: `媒体播放 Play` / `媒体暂停 Pause` / `媒体下一首 Next` / `媒体上一首 Prev`
    - 网页媒体按钮只在 `/events` 已实际发现 button 实体 id 后调用 `/button/<媒体按钮>/press`, 旧固件或 button 未广播时直接回退到 `select/媒体控制 Media`
    - button 探测失败不弹出跨源/IP错误, 避免未 OTA 到新固件时误报 `媒体指令失败，请检查IP/跨源权限`
    - 固件新增 `emit_media_action` 脚本和 4 个 template button, 每次 press 都发布 `ACTION#seq` HID 脉冲事件, 不再依赖 select 当前 option 是否变化
    - `send_volume_to_pga` 在 `anti_pop_fade_to_silence` 运行期间强制保持 `target_db=-96.0f`, 防止音量/取消静音动作把软降目标抢回当前音量
    - `soft_mute_switch.turn_off_action` 遇到防爆音软降进行中时只保持 `soft_mute=true` 并同步开关状态, 不提前恢复音量/打开硬件静音
    - MSGEQ7 零漂 offset 上限从 `320` 恢复为 `3600`, 接受实机 3.3V 下约 `3250 raw` 的直流偏置, 避免 offset 被丢弃后频谱常满格/信号判断失真
    - MSGEQ7 频段增益移到 4095→255 缩放之前, `NOISE_GATE` 降为 `2`, 避免几十 raw 的弱音频变化被整数截断后全部归零
    - 频谱显示静默消隐阈值降为 `peak<=1 && avg<=0`, 配合驱动弱信号输出
    - 固件显示版本号更新为 v2.1.33, 网页控制台显示更新为 WEB v2.1.33
  原因: 设备 REST 入口用 curl 实测返回 200, `/events` 也能看到实体, 说明固件 web_server 可控; 失败点集中在浏览器侧的 `fetch/no-cors` 跨源/PNA 发送不可靠, 以及旧页面在 SSE 抖动时禁用按钮。媒体控制方面, ESPHome `/events` 后续精简包会缺少 `name/domain`, 旧网页把 `eMap.media` 覆盖空后第二次点击会报 `未找到实体: media`; 同时 select 不是可靠的瞬时按钮。防爆音方面, 实机日志曾出现 `fade timeout before hard mute: current_db=-36.00 target_db=-36.00`, 说明软降脚本等待 `-96dB` 时被取消静音/音量路径抢回目标。MSGEQ7 方面, 实机 raw 直流偏置约 `3250`, 旧 offset 上限 `320` 会丢弃正常偏置。
  备注: 本次不修改 BLE HID 配对/解析逻辑。
改动文件:
  www/index.html
  智能前级蓝牙2.0.yaml (firmware_version, emit_media_action, media template buttons, soft_mute_switch, send_volume_to_pga, MSGEQ7显示消隐)
  PGA/msgeq7.h (offset上限, 增益/门限顺序)
  CHANGELOG.txt
  AGENTS.md
  README.md
  CLAUDE.md

---
v2.1.30 - 2026-07-19
  修复: 本地网页仍可能无法控制设备
    - www/index.html 的 POST body 从字符串 `1` 改为 `new Uint8Array([49])`
    - 继续提供非空 body/Content-Length, 满足 ESPHome 2026.7 `web_server_idf` 要求
    - 避免浏览器自动附加 `Content-Type: text/plain;charset=UTF-8` 后触发 CORS/OPTIONS 预检
    - 实机验证: 设备对普通 POST + 非空 body 返回 200, 但对 OPTIONS 预检返回空回复; 因此网页必须尽量保持简单请求
    - 网页控制台显示更新为 WEB v2.1.30, 便于确认浏览器没有加载旧页面
  原因: ESPHome 2026.7 的 REST 入口要求 POST 带 Content-Length; 同时设备 web_server 对 OPTIONS 预检支持不完整。字符串 body 虽然可解决 411, 但部分浏览器可能因 Content-Type 进入预检路径, 造成命令行 curl 可控、网页按钮无效。
  备注: 本次只修改本地网页发送方式和文档, 不修改固件音频逻辑、BLE HID 遥控器逻辑。
改动文件:
  www/index.html
  CHANGELOG.txt
  AGENTS.md

---
v2.1.29 - 2026-07-19
  修复: 本地网页按钮点击后设备仍不执行, 以及媒体按钮同一动作只能触发一次
    - www/index.html 的 POST 请求 body 从空字符串改为非空占位符 `1`, 强制浏览器发送 Content-Length
    - CORS 和 no-cors 两条发送路径都使用同一个非空 body, 避免兜底请求仍被设备拒绝
    - 新增 `hid_event_seq` 全局序号, 网页/手机 BLE/实体遥控发布 HID 事件时使用 `ACTION#seq` 作为 text_sensor 状态
    - `homeassistant.event: esphome.hid_events` 保持 `usage=ACTION`, 另加 `seq`, 避免破坏旧自动化
    - 固件显示版本号更新为 v2.1.29, 网页控制台显示更新为 WEB v2.1.29
  原因: 实机 `/events` 日志明确报 `Content length is required for post`, 本机直测无 body 的 POST 返回 411, 加 `--data-raw "x"` 后同一路径返回 200。继续连续两次播放测试时, REST 都 200 但 `BLE HID 按键事件` 固定为 `PLAY`, 同状态重复发布无法稳定形成第二次前端/HA触发, 因此需要事件脉冲序号
  备注: 本次不修改 BLE HID 配对/解析逻辑, 只改变 HID 事件 text_sensor 的状态格式; HA 事件里的 `usage` 仍为原始动作名
改动文件:
  www/index.html
  智能前级蓝牙2.0.yaml (firmware_version, hid_event_seq, BLE HID 事件发布)
  CHANGELOG.txt
  AGENTS.md

---
v2.1.28 - 2026-07-19
  修复: 本地网页按钮仍可能无法控制设备
    - www/index.html 的静音按钮改为走已发现/兜底的 `switch/静音-SW` 实体路径, 不再单独硬编码 switch 名称
    - 开机/关机/待机按钮直接操作 `switch/待机模式` 的 `turn_off/turn_on`, 不再绕 `select/媒体控制 Media`
    - 媒体按钮改为复用统一 `sendCmd('media', ...)`, 使用 SSE 发现到的实体名称路径和固定兜底
    - REST 控制先按普通 CORS POST 发送; 若浏览器因 CORS/PNA 判定失败, 自动退到 `no-cors` 方式发送同一条 POST
    - 固件显示版本号更新为 v2.1.28, 网页控制台显示 WEB v2.1.28
  原因: 实机 curl 直测 `number/主音量 Volume`、`select/媒体控制 Media`、`switch/待机模式` 均返回 200, 证明设备 REST 入口可用; 浏览器本地 HTML 仍可能被跨源/PNA 预检或前端硬编码路径拦住。ESPHome 2026.7 的 PNA 预检会在默认 `Access-Control-Allow-Origin: *` 外追加请求源, 个别浏览器会把这种双 ACAO 判为失败, 因此网页需要发送兜底并减少绕路
  备注: 本次不修改 BLE HID 遥控器和音频核心逻辑
改动文件:
  www/index.html
  智能前级蓝牙2.0.yaml (firmware_version)
  CHANGELOG.txt
  AGENTS.md

---
v2.1.27 - 2026-07-19
  修复: 防爆音输入切换路径和 MSGEQ7 有信号但显示/自动判断无信号
    - 输入切换从固定 1200ms 等待改为复用 anti_pop_fade_to_silence, 与静音/待机/输出模式一致: 软降到 current_db <= -95.5f 后再硬静音和切继电器
    - anti_pop_fade_to_silence 增加 DEBUG 级起止日志, 便于临时打开 logger 验证软降耗时和硬切前电平
    - MSGEQ7 启动零漂 offset 上限从 3600 raw 改为 320 raw, 避免带信号开机时把音乐电平采成 offset 后全部扣成 0
    - MSGEQ7 信号强度改用未显示消隐前的实时频谱平均值, 静音时也继续更新, 不再沿用旧的 0 值
    - 频谱显示静默消隐阈值从 peak<=36/avg<=16 降为 peak<=12/avg<=4, 避免小信号被当作无信号
    - msgeq7.h 增加 DebugFrame 只读快照, 仅供 DEBUG 日志验证 raw/offset/frame 三层数据
    - 网页/手机 BLE 的媒体控制命令现在会复用 `ble_hid_event_text` 推送 `esphome.hid_events`, 播放/暂停/上一首/下一首与 BLE HID 遥控保持同一条 HA 事件通道
    - 网页控制台版本显示更新为 WEB v2.1.27
  原因: v2.1.24 已修复静音/待机/输出模式的软降硬切顺序, 但输入切换仍保留旧 1200ms 等待; 按当前渐变速度, 常用音量降到 -96dB 可能超过 1.2 秒而提前硬切。MSGEQ7 方面, 过高的 offset 采样上限会在有信号开机时把真实信号当底噪扣除, 同时显示消隐阈值偏高、静音时信号值冻结会放大"无信号"现象
  备注: DEBUG 日志默认不会输出; 最终固件 logger 仍保持 INFO
改动文件:
  智能前级蓝牙2.0.yaml (firmware_version, anti_pop_fade_to_silence, switch_input, MSGEQ7 interval)
  PGA/msgeq7.h
  www/index.html
  CHANGELOG.txt
  AGENTS.md

---
v2.1.26 - 2026-07-19
  修复: 本地 www/index.html 网页仍无法控制设备
    - web_server 增加 allowed_origins: "*" 和 enable_private_network_access: true, 放行浏览器跨源/PNA 控制请求
    - 固件显示版本号更新为 v2.1.26
    - 网页控制台显示 WEB v2.1.26, 方便确认浏览器没有打开旧缓存
    - 网页实体映射增加固定实体名称兜底, 即使 SSE 状态尚未完成映射也能发 REST 控制请求
    - 静音按钮取消点击时本地假切换, 改为只根据设备 SSE 状态显示真实静音状态
    - 网页 POST 统一走 sendRequest, 控制失败时给出 toast 提示, 方便区分 IP/跨源权限问题
    - 移除不存在的 manifest/icon 引用, 并在打开时清理旧 Service Worker, 避免旧缓存或 sw.js 404 误导排查
  原因: 命令行不带 Origin 的 REST 请求可以 200, 但浏览器本地网页会带 Origin/PNA 预检; ESPHome 2026.7.0 默认未放行外部网页来源, 导致按钮请求被设备 web_server 拦截。本地 file:// 页面会产生 Origin: null, 不能单独写入 allowed_origins, 因此使用 "*" 覆盖本地文件和本地服务两种打开方式
  备注: 本次只修复网页控制链路, 不修改 BLE HID 遥控器和音频防爆音逻辑
改动文件:
  智能前级蓝牙2.0.yaml (firmware_version, web_server 跨源/PNA)
  www/index.html
  CHANGELOG.txt
  AGENTS.md

---
v2.1.25 - 2026-07-18
  修复: ESPHome 2026.7.0 下本地网页控制台无法控制和音量显示不同步
    - www/index.html 的 REST 控制路径改为使用 ESPHome web_server 接受的实体名称路径, 不再把 SSE 短 id 当作 REST object_id
    - POST 请求显式带空 body, 避免部分客户端触发 411 Length Required
    - 默认设备 IP 改为 192.168.31.99, 并把浏览器里遗留的旧默认 192.168.31.86 自动迁移到当前 IP
    - 网页面板音量 dB 换算改为与固件一致: PGA 寄存器值按 (reg - 192) * 0.5 显示
    - 待机圆形按钮改为显示"运行/待机", active 状态仅表示设备处于待机
    - 固件待机模式开关改为正向语义: ON=待机, OFF=运行, 同步修正媒体开机/关机、BLE 手机命令、温度保护恢复和 HID 电源键调用
    - 外部 select/输入源选择 Input 请求重新触发 switch_input, 目标等于 current_input 时直接跳过, 避免回到 v2.1.8 前的重复继电器吸合
    - 固件显示版本号更新为 v2.1.25
  原因: ESPHome 2026.7.0 的 web_server REST 接口使用实体名称路径; 原网页仍使用 SSE id 路径导致 404。v2.1.8 为修复重复切换移除了 input_select set_action 的 switch_input, 使网页/HA 外部选输入只改显示不切继电器。旧 standby_switch 使用 ON=运行/OFF=待机, 与实体名"待机模式"相反, 容易误操作
  备注: 本次未修改 BLE HID 遥控器代码
改动文件:
  www/index.html
  智能前级蓝牙2.0.yaml (firmware_version, input_select set_action)
  CHANGELOG.txt
  AGENTS.md

---
v2.1.24 - 2026-07-18
  加固: 防爆音状态机最终化, 避免软降超时或并发操作导致再次硬切
    - 新增 anti_pop_fade_to_silence 统一软降脚本, 静音/待机/输出模式切换共用同一条软降等待路径
    - 软降等待窗口延长到 5 秒, 只有脚本结束后才进入硬静音/继电器切换; 超时会记录 current_db/target_db
    - 忙碌期间开启静音只记录 soft_mute 请求, 不再在 switching_input/power_transitioning 中途直接硬静音
    - 进入待机时避开输入/输出切换进行中的窗口, 防止 switch_output_mode 和 enter_standby 互相覆盖状态
    - 输出模式切换恢复阶段保留用户在切换期间发出的静音请求, 不再强制恢复到切换前状态
    - 固件显示版本号更新为 v2.1.24, 方便实机和备份固件区分
  原因: v2.1.23 初版已能编译, 但 wait_until 超时后仍会继续硬切, 并发切换时也可能覆盖 soft_mute 状态
改动文件:
  智能前级蓝牙2.0.yaml (firmware_version, anti_pop_fade_to_silence, soft_mute_switch, enter_standby, switch_output_mode)
  CHANGELOG.txt
  AGENTS.md

---
v2.1.23 - 2026-07-18
  修复: 基于 v2.1.8 重新实现防爆音路径, 修复开启静音/进入待机/变压器直通切换硬切爆音
    - 开启静音: 先让 send_volume_to_pga 渐变到 -96dB, 等 current_db <= -95.5 后再打开硬件静音
    - 进入待机: power_transitioning 期间允许 PGA 渐变, 等软降完成后再 pga2311::set_volume(0,0)、硬静音、关背光
    - 输出模式: 新增 switch_output_mode 脚本, 按输入切换同类顺序执行软降 -> 硬静音 -> 切 GPA4 -> 恢复
    - HA set_mute 服务改走 soft_mute_switch, 不再直接操作 mute_switch
  原因: 之前爆音修复没有生效, 根因是多个入口仍在渐变前直接硬件静音/继电器切换/重写 current_db
改动文件:
  智能前级蓝牙2.0.yaml (soft_mute_switch, enter_standby, send_volume_to_pga, switch_output_mode)
  CHANGELOG.txt
  AGENTS.md

---
v2.1.22 - 2026-07-17
  恢复: 回退到 v2.1.8 代码基线(无爆音修复), 保留以下修复:
    - 修复黑屏: mipi_spi 显式 spi_mode: 0 (NV3007 需要 MODE0, ESPHome 2026.6.4+ 默认 MODE3)
    - 兼容 ESPHome 2026.7.0: platformio_options.build_flags → build_flags (废弃格式)
  回退原因: 用户要求回退到 v2.1.8 备份点, 只叠加黑屏修复
改动文件:
  智能前级蓝牙2.0.yaml (spi_mode: 0 + build_flags 格式)
  CHANGELOG.txt

---
v2.1.8 - 2026-07-04
  修复: 输入切换继电器吸合两次
    - 根因: input_select 的 set_action + 调用方各调了一次 switch_input
    - 修复: 移除 set_action 中的 switch_input->execute()（第6578行）
    - 调用方直接调用 switch_input 一次即可，set_action 只负责参数校验
改动文件:
  智能前级蓝牙2.0.yaml (input_select set_action)
  CHANGELOG.txt

---

v2.1.7 - 2026-07-02
  ESPHome 升级: 2026.5.3 → 2026.6.4
    - pip install --upgrade esphome
    - 现有代码完全兼容，无需额外适配
  屏幕 SPI 频率提升: 40MHz → 80MHz
     - NV3007 TFT SPI 时钟频率翻倍，提升 UI 刷新流畅度
  固件版本号更新: v2.1.0 → v2.1.5
改动文件:
  AGENTS.md (ESPHome 版本号同步)
  CHANGELOG.txt

---

v2.1.6 - 2026-07-02
  工厂重置逻辑修正: 仅待机状态允许 10s 重置触发
    - 新增 started_from_standby 静态变量记住按下瞬间的待机状态
  屏幕 SPI 频率提升: 40MHz → 80MHz
    - NV3007 TFT SPI 时钟频率翻倍，提升 UI 刷新流畅度
  固件版本号更新: v2.1.0 → v2.1.5

改动文件:
  智能前级蓝牙2.0.yaml
  CHANGELOG.txt

---

    - 10s 触发条件增加 && started_from_standby 守卫
    - 从非待机状态长按不会再误触工厂重置
  字体修复: factory_msg 缺字显示空白框
    - 在 font_cn / font_cn_small 的字形列表追加 5 个字符: 继续按住请热
    - 影响消息: "继续按住恢复出厂设置..." + "已重置，请连接热点"

改动文件:
  智能前级蓝牙2.0.yaml
  CHANGELOG.txt

---

v2.1.4 - 2026-07-02
  工厂重置修复: 长按10s WiFi 重置失效
    - 原因: clear_sta() 只清内存 STA 配置，ESPHome 开机从 NVS 重新加载旧凭证
    - 修复: 改用 save_wifi_sta('', '') 将空凭证持久化到 NVS，重启后设备进入 AP 配网模式
    - 同时修复: start_wifi_ap 脚本中的相同问题

  UI 修复: 长按 5s 工厂重置提示层级修复
    - 5s 提示改为 lv_scr_load_anim + lv_obj_move_foreground 确保显示在最顶层
    - 工厂重置时清除 BLE 配对 BDA
  
  编译修复: LvPageType* 与 lv_obj_t* 类型不匹配
    - id(main_page) 改为 id(main_page)->obj 适配 ESPHome LVGL managed component

改动文件:
  智能前级蓝牙2.0.yaml
  CHANGELOG.txt

---
v2.1.3b - 2026-07-02
  编译修复: Montserrat 字体从 Google Fonts 在线下载改为本地 fonts/Montserrat-Regular.ttf
    - 原因: Google Fonts 下载超时导致编译失败
    - 改动: 智能前级蓝牙2.0.yaml 中 8 个 Montserrat 字型全部改为本地文件

v2.1.3 - 2026-07-02
  UI修复: mdi_settings_icons_24 字体缺少4个MDI图标码位导致图标不显示
    - 新增 U+F03E4 (暂停图标)
    - 新增 U+F0652 (学习按键图标)
    - 新增 U+F0450 (重置学习图标)
    - 新增 U+F0415 (MSGEQ7校准图标)

╔══════════════════════════════════════════════════════════════════╗
║        智能前级2.0 — 固件迭代记录 (ESP32-S3 + ESPHome)          ║
║        最后更新: 2026-05-31 (迭代14)                                     ║
╚══════════════════════════════════════════════════════════════════╝

══════════════════════════════════════════════════════════════════════
【迭代14】2026-05-31 — BLE HID Host 双角色状态机加固
══════════════════════════════════════════════════════════════════════
目标: 复核 ESP32-S3 同时运行 BLE Server + BLE HID Client 的运行时边界，修复遥控器扫描/断开/重连链路的隐性问题。

修改文件:
  PGA/ble_hid_host.h     — BLE HID Host 扫描与断线状态机修复
  README.md              — BLE 遥控架构说明同步
  CLAUDE.md              — BLE 遥控架构说明同步
  CHANGELOG.txt          — 本文件

  ▸ 🔴 关键修复
    - 扫描流程改为等待 `ESP_GAP_BLE_SCAN_PARAM_SET_COMPLETE_EVT` 后再调用 `esp_ble_gap_start_scanning()`，
      避免 Bluedroid 扫描参数尚未生效时立即启动扫描导致偶发失败。
    - 统一处理 `ESP_GATTC_OPEN_EVT` 连接失败、`ESP_GATTC_DISCONNECT_EVT` 远端断开、
      `ESP_GATTC_CLOSE_EVT` 本地关闭，防止遥控器关机/离场后状态卡在“已连接”且不再自动重连。
    - 自动重连调度抽成同一逻辑，连接失败也会进入 2s/5s/8s/60s 退避重试。

  ▸ 🟡 兼容性修复
    - 扫描结果同时解析 Advertising Data 与 Scan Response，兼容把设备名或 HID UUID 放在 Scan Response 的遥控器。
    - 增加 `scan_start_pending`，停止扫描时可取消尚未真正启动的异步扫描请求。

  ▸ ✅ 验证
    - `esphome config 智能前级蓝牙2.0.yaml` 通过。
    - `esphome compile 智能前级蓝牙2.0.yaml` 通过，Flash 29.9%, RAM 23.8%。

══════════════════════════════════════════════════════════════════════
【迭代13】2026-05-31 — MCP23017 中断配置显式化
══════════════════════════════════════════════════════════════════════
目标: 消除 ESPHome 对 MCP23017 扩展 IO 的误导性 polling warning，明确音频检测仍由 MCP23017 Hub 中断驱动。

修改文件:
  智能前级蓝牙2.0.yaml  — 音频检测 GPB0~GPB3 显式 `interrupt: CHANGE` + `use_interrupt: false`
  README.md              — 维护说明同步
  CLAUDE.md              — 维护说明同步
  CHANGELOG.txt          — 本文件

  ▸ 🟡 配置澄清
    - `mcp23017.interrupt_pin: GPIO12` 保持不变，MCP23017 INTB 仍为中断入口。
    - 四路音频检测 pin 显式设置 `interrupt: CHANGE`，由 MCP23017 在输入变化时拉低 INTB。
    - `gpio binary_sensor.use_interrupt: false` 仅关闭 ESP32 原生 GPIO ISR 尝试；
      不是关闭 MCP23017 中断，可避免 “Pin is not an internal GPIO, falling back to polling mode” 提示。

  ▸ ✅ 验证
    - `esphome config 智能前级蓝牙2.0.yaml` 通过，MCP23017 polling warning 已消失。

══════════════════════════════════════════════════════════════════════
【迭代12】2026-05-31 — 编码器旋转路径收口修复
══════════════════════════════════════════════════════════════════════
目标: 修复编码器旋转在非音量页面误触发音量脚本/大字音量页，以及待机/温保时旋转仍改音量的边界问题。

修改文件:
  智能前级蓝牙2.0.yaml  — 编码器 on_value 后续 action 收口到同一 lambda
  README.md              — 设置页 13 项与编码器交互文档同步
  CLAUDE.md              — 设置页 13 项与编码器维护说明同步
  CHANGELOG.txt          — 本文件

  ▸ 🔴 关键修复
    - 将编码器旋转后的 `send_volume_to_pga` / `show_volume_big` / `reset_idle_timer`
      收进同一个 lambda，避免 BLE 遥控页、遥控器按键页、诊断页旋转后继续执行外层 action。
    - 音量旋转入口增加 standby/temp_protect/system_ready 守卫，待机和温保期间不再改 `volume_val` 或弹出大字音量。

  ▸ 🟢 文档同步
    - 设置页文档由 12 项更新为 13 项，补齐 Row 10 输出模式，固件版本/IP 调整为 Row 11/12。

══════════════════════════════════════════════════════════════════════
【迭代11】2026-05-31 — 自动化状态机审计与温保互锁修复
══════════════════════════════════════════════════════════════════════
目标: 复核复杂自动化入口，修复 lambda guard 后续 action 继续执行、温保唤醒、自动输入延迟误触发等边界问题。

修改文件:
  智能前级蓝牙2.0.yaml  — 自动化入口/温保/自动输入切换/输出模式显示修复
  partitions.csv         — 分区表注释改为 ASCII，避开 Windows/ESP-IDF 编码解析风险
  README.md              — 项目文档同步
  CLAUDE.md              — 维护说明同步
  CHANGELOG.txt          — 本文件

  ▸ 🔴 关键修复
    - API service 与 HA number/select 入口改为在 lambda guard 内部执行后续副作用，
      避免 `return` 后外层 `script.execute` 仍继续运行。
    - 温度保护增加 `temp_protect_forced_standby`，保护期间禁止 HA/BLE/编码器唤醒；
      降温后只恢复本次被温保强制拉入待机的运行状态。
    - `auto_switch_input` 增加 `auto_switch_pending` 闩锁，只有真实进入 8 秒等待期后才允许二段切换，
      防止无效启动留下的延迟段误切输入。

  ▸ 🟠 操作与显示修复
    - BLE App 输入切换命令记录 `last_manual_input_ms`，与 HA/编码器手动选择规则一致。
    - 设置页输出模式切换时同步 transformer/direct 图标隐藏状态。
    - 工厂重置同步清理温保/自动切换闩锁，并恢复输出模式继电器默认状态。

  ▸ 🟢 审计结论
    - 未修改 `send_volume_to_pga` 内部 while + 15ms 渐变核心逻辑。
    - 复核 standby/exit_standby、BLE HID、电源 select、自动输入检测触发路径，未发现新的代码级硬伤。
    - `esphome config 智能前级蓝牙2.0.yaml` 通过。
    - `esphome compile 智能前级蓝牙2.0.yaml` 通过，Flash 29.9%, RAM 23.9%。

══════════════════════════════════════════════════════════════════════
【迭代10】2026-05-31 — 显示/操作互锁审计修复
══════════════════════════════════════════════════════════════════════
目标: 修复显示页状态、输入切换音量记忆、设置/按键映射页调节状态残留等问题。

修改文件:
  智能前级蓝牙2.0.yaml  — 显示/互锁/操作逻辑修复
  PGA/msgeq7.h          — 重校准日志参数修复
  README.md             — 项目文档同步
  CLAUDE.md             — 维护说明同步
  CHANGELOG.txt         — 本文件

  ▸ 🔴 关键修复
    - 输入切换新增 active_input，保存旧音量时使用实际接通通道，
      避免先修改 current_input 后把旧通道音量写入目标通道的 input_vol_*。
    - send_volume_to_pga 增加 standby 强制静音守卫，待机期间不会被其它路径恢复音量。
    - soft_mute 关闭动作在 standby/temp_protect/OTA 中保持硬静音，
      防止保护/待机期间误开硬件静音继电器。

  ▸ 🟠 显示与页面状态修复
    - display_idle_timer、reset_idle_timer、show_volume_big 的 LightCall 补齐 perform()。
    - lvgl on_resume 恢复背光时显式使用 display_brightness。
    - 删除 settings_page 30s on_idle 回主页路径，统一由 interval 的 10s 回页逻辑清理状态。
    - 静音时频谱自动切换不再进入/退出，避免 s_signal_avg 冻结后误跳频谱页。
    - BLE 遥控未配对/隐藏时同步隐藏状态栏电池条。

  ▸ 🟡 操作逻辑修复
    - settings_page / remote_keys_page 超时或长按回主页时清理 active_idx 和行背景/bar 高度。
    - 进入 remote_keys_page 时重置焦点、调节态和行高亮，避免上次状态残留。
    - debug_page 中 MSGEQ7 重校准后停留在诊断页显示结果，只有返回行才回设置页。
    - 输入选择、BLE HID 切换输入、BLE Server 输入命令增加 switching_input 防重入守卫。

  ▸ 🟢 诊断与文档
    - msgeq7::recalibrate() 日志参数与 7 个频段 offset 对齐。
    - README/CLAUDE 同步 active_input、页面状态清理、双击静音有效范围和诊断页行为。

══════════════════════════════════════════════════════════════════════
【迭代9】第三轮审计修复 — 编译错误 + YAML 结构 + 字体覆盖 + 三击bug
══════════════════════════════════════════════════════════════════════
目标: 修复第三轮全面审查中发现的 9 项缺陷（3项编译阻断/功能崩溃、3项高危、2项中等、1项轻微）

修改文件:
  智能前级蓝牙2.0.yaml  — 9处修改
  CHANGELOG.txt         — 本文件

  ▸ 🔴 严重修复 (3项)
    - C1: lbl_ble lambda 尾部多余 } 删除 (编译阻断, 第1269行)
    - C2: ble_remote_page HID调试标签与返回按钮YAML结构修复
         (分离空 obj + label id 覆盖为两个独立 widget)
    - C3: 三击进入诊断页功能修复 — 将debug跳转移入 on_multi_click 三击处理器
         (原实现依赖单次单击handler中的fw_click计数器, 但ESPHome on_multi_click
         匹配三击后跳过单击, 导致计数器永不递增, 功能完全失效)

  ▸ 🟠 高危修复 (3项)
    - H1: font_cn_small 增加"电池"二字 (BLE 状态文字"电池:50%"渲染为空白框)
    - H2: 删除三处冗余 guard_ok = false; 赋值 (第422/4073行)
    - H3: standby_switch 添加命名注释 (历史旧约定: HA switch ON=运行中, OFF=待机模式; v2.1.25 已改为 ON=待机)

  ▸ 🟡 中等修复 (1项)
    - M1: reset_idle_timer 熄屏亮度恢复补充 .perform() (确保立即生效)

  ▸ 🟢 轻微修复 (1项)
    - L1: MCP23017 scan: true → false (节省 ~100ms 启动时间)

  ▸ 审计发现 (记录在案, 未修改)
    - M4: balance 无总增益补偿 (前级惯例, 左减右不加, 保持现状)
    - M2: HA 内存传感器 60s 推送延迟 (与 UI 20Hz 刷新分离, 设计合理)
    - M3: 顶栏 ~150px 空白 (仪表盘风格, 布局预期)

══════════════════════════════════════════════════════════════════════
【迭代8】52项严审修复 — 安全加固 + 竞态修复 + UI完整性

══════════════════════════════════════════════════════════════════════
【迭代8】52项严审修复 — 安全加固 + 竞态修复 + UI完整性
══════════════════════════════════════════════════════════════════════
目标: 三轮全量审计（YAML BLE/编码器/脚本 + YAML LVGL/显示/设置 + C++ 头文件驱动），
      修复 22 项安全/功能/逻辑缺陷。

修改文件:
  智能前级蓝牙2.0.yaml  — 20处修改 (BLE守卫/CRC/编码器/VU主题/splash/字体等)
  PGA/pga2311.h         — 空指针防护 (set_volume隐式setup失败后return)
  PGA/msgeq7.h          — _ntc_smooth临界区保护 + NTC温度有效范围收紧
  PGA/lvgl_compat.h     — LV_CONF_SKIP无条件定义 (修复include顺序依赖)
  PGA/ble_hid_host.h    — TOCTOU修复 + dropped_events原子保护 + 局部变量初始化
  CHANGELOG.txt         — 本文件

  ▸ 🔴 严重修复 (5项)
    - C1: factory_reset 中 power_on_limit 160→110 (防止恢复出厂后开机音量过高)
    - C3: pga2311::set_volume() 隐式setup()失败后空指针检查 (防止SPI崩溃)
    - C4: ble_hid_host::connect() discovered_count 锁外读取→移入临界区 (TOCTOU)
    - C5: font_cn 字形从35字符扩展到70+ (修复splash/BLE/设置/主题名全部空白)
    - ✗ C2: send_volume_to_pga while循环保留 (CLAUDE.md禁止修改, 已验证安全)

  ▸ 🟠 高危修复 (4项)
    - H2: LVGL long_press_time 800ms→5000ms (避免与binary_sensor 1500ms长按冲突)
    - H3: 三击进入诊断页/BLE页导航前补充 reset_idle_timer (防页面立即弹回)
    - H4: BLE HID CYCLE_INPUT 增加 1500ms 防抖 (与其它输入切换路径一致)
    - H5: LV_CONF_SKIP 移除 #ifndef LV_CONF_H 条件 (修复include顺序依赖bug)
    - ✗ H1: Web Server认证未添加 (局域网设备, 认证破坏HA集成便利性)

  ▸ 🟡 中等修复 (13项)
    - M1: 编码器 diff 范围限制 [-10,10] (防int32_t翻转导致音量突变)
    - M2: HA volume_number set_action 增加 v<0 下界检查
    - M3: 软静音时 s_signal_avg 保持上次值 (防频谱页因静音提前退出)
    - M4: 设置页输入选择用模运算 (防快旋跳过中间值)
    - M5: BLE balance 命令 int8_t→int 显式转型 (消除实现定义行为)
    - M6: guard_ok 去 static (2处, 消除脆弱的状态传递)
    - M7: HID CYCLE_INPUT 同步发布 HA input_select (之前有~200ms延迟)
    - M8: apply_theme 增加 VU 电平表指示器着色 (之前始终绿色)
    - M9: splash 渐显最终帧 opa=255 (之前最大仅231/255≈90%)
    - M11: 温度保护解除后恢复为theme accent色 (之前固定为灰色)
    - M13: msgeq7 _ntc_smooth 读写移入临界区 (与get_temperature()竞争)
    - M14: ble_hid_host dropped_events 递增用portENTER_CRITICAL保护
    - M15: ble_hid_host 未初始化局部变量初始化 (2处 CLOSE_EVT/SEARCH_CMPL_EVT)
    - ✗ M10: LVGL on_idle 30s死代码保留 (安全网, 无害)
    - ✗ M12: read() 隐式setup线程安全保留 (启动顺序已保证单线程)

  ▸ 🟢 轻微修复 (1项)
    - L13: 熄屏亮度 3%→10% (保持高级音响质感, 隐约可见显示内容)

  ▸ 审计发现 (本轮未修, 记录在案)
    - L2-L27: 27项低优先级代码质量/风格发现 (详见审计报告)
    - C2: send_volume_to_pga全幅渐变~825ms (受CLAUDE.md保护, 已验证异步安全)

  ── 第二轮补充修复 (第二轮审计后补 11 项) ──

  🔴 严重修复 (5项)
    - C1: montserrat_36 字体添加 (splash_page 品牌标题编译阻断)
    - C2: font_cn 追加 继续按住恢复出重置请连接热点 (恢复出厂显示空白)
    - C3: font_cn_small 追加 内堆部小空闲运行间重 (诊断页/BLE状态空白)
    - C4: 三击 pattern 先于双击匹配 + 双击静音加入 volume_big_page
    - C5: exit_standby 增加 brightness 恢复 (唤醒后残留 10% 暗屏)

  🟠 高危修复 (4项)
    - H1: ble_remote_page 增加 HID 原始事件调试标签 (对接已有 raw event API)
    - H2: lvgl_compat.h 添加 get_theme_accent() 辅助函数 (消除 7 处重复 switch)
    - H3: 移除编码器旋转/单击 handler 中冗余 settings_ui::init() 调用 (仅保留进入设置页)
    - H4: 双击静音条件加入 volume_big_page (调节音量时可静音)

  🟡 中等修复 (2项)
    - M1: volume_big_page bg_opa 0%→90% (防止主页内容透出)
    - M4: 诊断页运行时间追加秒数显示 (原仅天/时/分)

  🟢 轻微修复 (1项)
    - L13: 熄屏亮度 3%→10% (保持高级音响质感) [与上轮重复, 记录备查]

══════════════════════════════════════════════════════════════════════
【迭代7】诊断信息页 + BLE 遥控页完善 + 三击隐藏入口
══════════════════════════════════════════════════════════════════════
目标: 新增诊断页(内存/CPU监控)，完善BLE遥控页交互，修复多页空闲自动返回

修改文件:
  智能前级蓝牙2.0.yaml  — 新增 debug_page/ble_remote_page 逻辑、HA内存传感器
  PGA/ble_hid_host.h    — BLE HID Host 驱动 (ESP32-S3 双角色)
  PGA/settings_ui.h     — 更新 N=11 适配设置页行数
  preview/index.html    — 五屏模拟器 (main/spectrum/settings/ble_remote/debug)
  CHANGELOG.txt         — 本文件

  ▸ 新增第4页 ble_remote_page (蓝牙遥控配对页)
    - 独立页面: 标题/状态/扫描按钮/设备槽×3/HID事件调试/断开或返回
    - 编码器焦点导航: 0=扫描, 1~3=设备, 4=返回 (动态隐显)
    - 扫描模拟: 1秒间隔发现设备, 3秒超时停止
    - 连接模拟: 随机电池电量 65~94%, HID 事件显示
    - 状态栏: 耳机电量风格指示条 (蓝牙图标 + 14×7px 电池条)

  ▸ 新增第5页 debug_page (诊断信息页)
    - 内存监控: 内部堆 (320KB) / PSRAM (8MB) 实时占用显示
    - 低水位标记: 全时段最小空闲内存
    - CPU 占用率: 动态模拟 (2~35%, 3%概率尖峰至99%)
    - 运行时间: 天/时/分/秒

  ▸ 三击固件版本进入诊断页
    - 设置页 Row 9 (固件版本): 快速单击三次编码器 → debug_page
    - 预览 HTML: click-counting 移至 mouseup/touchend 原始事件处理器
    - 取消待处理 singleClick 定时器防止跳回
    - YAML: 新增 skip_idle_reset 全局变量
    - reset_idle_timer 检查 skip_idle_reset 防 main_page 覆盖

  ▸ 设置页重新排序
    - 蓝牙遥控从第10位提前到第8位 (Row 8)
    - 固件版本 Row 9, IP地址 Row 10
    - settings_ui.h N=11, 更新 init() 签名

  ▸ 编码器交互修正
    - 双击静音限制仅主页/频谱页有效
    - 设置页/BLE页 10秒无操作自动返回主页
    - 诊断页 30秒无操作自动返回主页
    - BLE页断开按钮留在本页 (不返回设置)
    - 设置页只读行(9,10)不进调节模式

  ▸ BLE HID Host 驱动优化
    - ESP32-S3 双角色 (BLE Server + Client 同时运行)
    - FreeRTOS 队列桥接 BLE 回调 → ESPHome 主循环
    - 电池电量读取 (Battery Service 0x180F)
    - HID 原始事件调试查看器
    - HA 事件推送 (homeassistant.event: esphome.hid_events)
    - 断开自动重试 3 次, 间隔逐次增大
    - 待机时 BLE 连接保持, 遥控器可唤醒
    - 所有 ESP-IDF v5.x 编译适配已修复

══════════════════════════════════════════════════════════════════════
【Release v2.0.0】2026-05-26
══════════════════════════════════════════════════════════════════════
面向最终用户的第一个正式版本。

  新增:
  ▸ 恢复出厂设置: 编码器长按 10s 触发，重置所有存储值 + 清除 WiFi + 重启
  ▸ 设置页: 输入自动切换开关（独立 UI）
  ▸ MCP23017 中断支持: open_drain_interrupt + INTB 中断检测
  ▸ settings_ui.h 集中管理设置页数组
  ▸ 8 种世界名机主题色（翠绿/赤红/天蓝/紫/麦景图蓝/金嗓子金/柏林青/南瓜橙）

  修正:
  ▸ P0-P2 全部 10 个 Bug 修复（峰值线重叠、LED 指示、消抖、ESPHome 2026 API 适配等）
  ▸ CLAUDE.md 文档全面修正（引脚映射、继电器顺序、blocking 描述纠正等）
  ▸ 频谱页自动切换: 从 MCP23017 引脚检测改为 MSGEQ7 真实频谱信号

  重要提醒:
  ▸ lvgl_compat.h 使用 extern "C" 绕过 opaque 限制，升级 ESPHome 大版本时需注意
  ▸ sdkconfig 强绑定 Octal PSRAM，Quad PSRAM 开发板会 Bootloop

文件清单:
  智能前级2.0.yaml  — 主配置 (ESPHome + LVGL UI)
  pga2311.h         — PGA2311 音量控制 IC 驱动 (SPI3_HOST)
  msgeq7.h          — MSGEQ7 七段频谱分析器 + NTC 温度驱动

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【迭代1】McIntosh "Blue Power" UI 重设计
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
目标: 参考麦景图(McIntosh)风格, 打造专业级 HiFi 前级 UI

修改文件: 智能前级2.0.yaml

  ▸ 色彩体系全面替换
    - 背景: #050510 深蓝黑 (原黑色)
    - 主文字: #D8E4F0 冷白 (原纯白)
    - 强调色: #0090E8 麦景图蓝光 (原普通蓝)
    - 次要文字: #4A6890 蓝灰
    - 频谱渐变: 7阶蓝色 #003868→#004888→#005CA8→#006EC0→#0080D8→#0094F0→#00A8FF
               (低音深蓝→高音亮蓝, 原为彩虹色)
    - 峰值线: #FFFFFF 白色 (专业表头风格)
    - 频谱轨道: #0C1628 深蓝底色

  ▸ 新增 UI 元素
    - 7 条白色峰值保持线 (peak_0~peak_6, h=2px, opa=180)
    - 7 个频率标签 (63/160/400/1k/2.5k/6k/16kHz)
    - 状态栏分隔线 (0x102040, y=15, h=1)

  ▸ 布局调整
    - 频谱条高度: 122→100px
    - 音量条高度: 匹配 100px
    - 频谱起始 x=68, 每条 w=46, 间距 6px

  ▸ 设置页更新
    - 背景色统一 0x050510
    - 滑块背景色 color_track (#0C1628)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【迭代2】MSGEQ7 硬件调试 (5项修正)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
目标: 解决 MSGEQ7 在 3.3V 供电下的时序/噪声/校准问题

修改文件: msgeq7.h

  ▸ 修正1: STROBE 延时加倍
    - STROBE 低电平延时: 20μs→40μs (3.3V 输出建立需 ≥36μs)
    - STROBE 高电平延时: 20μs→40μs
    - 原因: 3.3V 供电下模拟摆幅小, 输出建立比 5V 慢

  ▸ 修正2: 噪声门限提升
    - NOISE_GATE: 3→8
    - 原因: 3.3V 供电底噪约 100-200mV, 对应 ADC ≈ 8-15/255
    - 效果: 静音时频谱不再跳动

  ▸ 修正3: NTC 温度 IIR 低通滤波
    - 新增 _ntc_smooth 变量, NTC_IIR_ALPHA=0.15f
    - 时间常数约 7s (@10s 读取间隔)
    - 原因: 中值滤波后仍有 ±0.3°C 抖动

  ▸ 修正4: 确认 3.3V 供电安全
    - MSGEQ7 工作电压 2.7~5.5V, 3.3V 在规格内
    - ESP32-S3 GPIO 无需分压, 直连安全

  ▸ 修正5: 零漂校准
    - 新增 _r_offset[7] / _l_offset[7] 数组
    - setup() 丢弃前3帧期间采集偏置平均值
    - read() 中减去偏置后再缩放
    - 原因: 每片 MSGEQ7 静态偏置不同, 减去后静音更干净
    - 启动时 ESP_LOGI 输出偏置值供调试

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【迭代3】ESP32-S3 性能极限榨取
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
目标: 最大化 ESP32-S3 运行效率, 消除所有性能瓶颈

修改文件: 智能前级2.0.yaml + msgeq7.h + pga2311.h

  ── A. sdkconfig 硬件调优 (智能前级2.0.yaml) ──

    ▸ CPU 频率锁定 240MHz
      CONFIG_ESP32S3_DEFAULT_CPU_FREQ_240: "y"

    ▸ 缓存配置
      数据缓存: 32KB→64KB  (CONFIG_ESP32S3_DATA_CACHE_SIZE_64KB)
      指令缓存: 16KB→32KB  (CONFIG_ESP32S3_INSTRUCTION_CACHE_SIZE_32KB)
      缓存行:   32B→64B    (数据+指令, 提升 DMA/PSRAM 命中率)

    ▸ PSRAM 配置
      八线模式: CONFIG_SPIRAM_MODE_OCT
      80MHz:    CONFIG_SPIRAM_SPEED_80M
      指令取自 PSRAM: CONFIG_SPIRAM_FETCH_INSTRUCTIONS (释放 Flash 带宽)
      只读数据取自 PSRAM: CONFIG_SPIRAM_RODATA (字体 glyph 不抢 Flash)
      允许 malloc: CONFIG_SPIRAM_USE_MALLOC

    ▸ LVGL 渲染加速
      SIMD 指令: CONFIG_LV_USE_DRAW_SW_ASM
      硬件 FPU:  CONFIG_LV_DRAW_SW_ASM_ASSUME_VFP

  ── B. SPI 速度提升 (智能前级2.0.yaml) ──

    ▸ TFT 显示 SPI2_HOST: 40MHz→80MHz (ESP32-S3 SPI2 支持 DMA)
    ▸ 显示组件 data_rate: 40MHz→80MHz

  ── C. LVGL 缓冲区 (智能前级2.0.yaml) ──

    ▸ buffer_size: 25%→100% (全帧 428×142×2=121KB, PSRAM 容纳)
    ▸ 效果: 零撕裂渲染, 脏区单次传输

  ── D. 频段增益整数化 (msgeq7.h) ──

    ▸ float BAND_GAIN[7] → int BAND_GAIN_Q8[7]
      {230, 256, 282, 320, 371, 435, 512}
      等效增益: ×0.90, ×1.00, ×1.10, ×1.25, ×1.45, ×1.70, ×2.00
    ▸ 运算: (float)r_val * BAND_GAIN[i] → (r_val * BAND_GAIN_Q8[i]) >> 8
    ▸ 收益: 14 次 float 乘法 → 14 次 int 乘法+移位, 约 5x 加速

  ── E. 单锁快照更新 (智能前级2.0.yaml) ──

    ▸ 原来: 8 个 ESPHome interval action (14 次 get_combined/get_peak 加锁)
    ▸ 现在: 1 个 interval lambda
      - msgeq7::get_frame() 一次加锁拷贝 combined[7]+peak[7]+temperature
      - 直接 LVGL API: lv_bar_set_value(bar, val, LV_ANIM_OFF)
      - 值变化才更新: lv_bar_get_value() != val 时才 set
      - 峰值线位置: lv_obj_set_y() 直接设置

  ── F. PGA2311 SPI 时钟 ──

    ▸ 初版: 4MHz
    ▸ 性能迭代曾提至 10MHz (错误)
    ▸ 详见迭代4修正

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【迭代4】PGA2311 SPI 致命超频修正 ★安全修正★
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
修改文件: pga2311.h

  ▸ 问题: SPI 时钟从 4MHz 优化到 10MHz, 超出芯片绝对上限
  ▸ 依据: TI PGA2311 Datasheet Page 6, Timing Requirements
          fSCLK 最大 6.25MHz (时钟周期最少 160ns)
  ▸ 危险: 超频 60% 会导致移位寄存器错位
          数据错位时, 微小音量可能被解码为 0xFF (+31.5dB)
          → 瞬间满功率输出, 可烧毁喇叭/永久损伤听力
  ▸ 修正: clock_speed_hz: 10000000 → 4000000
  ▸ 理由: 4MHz 下 16bit 传输仅 4μs, 相比 15ms fade 步进可忽略
          音量渐变瓶颈不在 SPI 速度, 安全第一
  ▸ 安全范围: ≤6.25MHz (建议 ≤6MHz 留余量)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【迭代5】音频检测自动输入切换
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
目标: 当检测到音频输入时自动切换到对应通道, 多路同时输入按优先级选择

修改文件: 智能前级2.0.yaml

  ▸ 新增全局变量 auto_input
    - 类型: bool, restore_value=true, 默认 true (开启)
    - 控制自动切换功能的启用/禁用

  ▸ 新增脚本 auto_switch_input
    - 触发条件: 任意音频传感器状态变化 (on_press / on_release)
    - 防抖: 500ms 延迟等待所有传感器稳定 (mode: restart 自动重启计时)
    - 优先级: CD(0) > DAC(1) > PC(2) > AUX(3)
    - 切换逻辑:
      1. 检查 auto_input 开关 / system_ready / switching_input / standby
      2. 扫描所有 4 路音频传感器, 取最高优先级的活跃通道
      3. 所有通道无信号 → 保持当前输入 (不切走)
      4. 目标通道与当前相同 → 跳过
      5. 目标通道不同 → 执行 switch_input 脚本 (含继电器切换/音量渐变)

  ▸ 修改 4 路音频检测传感器 (audio_cd/dac/pc/aux)
    - 新增 on_press → script.execute: auto_switch_input
    - 新增 on_release → script.execute: auto_switch_input
    - 保留原有滤波: delayed_on 100ms / delayed_off 500ms

  ▸ 新增 HA 模板开关 auto_input_switch
    - 名称: "自动输入切换"
    - 图标: mdi:swap-horizontal
    - restore_mode: RESTORE_DEFAULT_ON
    - turn_on → auto_input=true
    - turn_off → auto_input=false

  ▸ 修改状态栏 lbl_audio
    - 新增 A:/M: 前缀指示自动/手动模式
    - A:CD DA PC AX = 自动模式
    - M:CD DA PC AX = 手动模式
    - x 位置调整: 88→70 (为前缀腾出空间)

  场景示例:
    - 仅 CD 有信号 → 自动切到 CD
    - CD+DAC 同时有信号 → 切到 CD (优先级高)
    - 当前在 CD, CD 停止, DAC 有信号 → 自动切到 DAC
    - 所有输入无信号 → 保持当前通道
    - auto_input=false → 不自动切换, 仅显示音频状态

  ▸ 修改状态栏音频通道指示
    - 原: 单个灰色 lbl_audio ("A:CD DA PC AX" / "-- ")
      有信号和无信号都是灰色, 难以区分
    - 新: 5 个独立标签
      lbl_auto_mode: A:/M: 前缀 (灰色, 指示自动/手动模式)
      lbl_sig_cd/da/pc/aux: 各通道独立指示
        有信号 → #0090E8 麦景图亮蓝 (醒目)
        无信号 → #1A2A40 深蓝黑 (几乎隐形)
    - 效果: 一眼可见哪个通道有音频输入

  ▸ 修改 WiFi / BLE 连接指示
    - WiFi 原: 连接="WiFi"绿色, 断开="----"红色 (不直观)
    - BLE 原: 连接="BLE"蓝色, 断开=""深色 (完全看不到)
    - 改为: 固定文字 + 颜色区分 (与音频通道指示统一风格)
      WiFi 已连接 → #00CC66 绿色, 未连接 → #2A3A50 暗灰 (始终显示 "WiFi")
      BLE  已连接 → #0090E8 蓝色, 未连接 → #2A3A50 暗灰 (始终显示 "BLE")
    - BLE x 位置调整: 380→385

  ▸ 状态栏全面图标化 (LED 圆点替代文字)
    - 设计理念: 专业 HiFi 设备 LED 指示灯风格, 亮=有效/暗=无效
    - 删除文字标签: lbl_mute(MUTE), lbl_auto_mode(A:/M:),
      lbl_sig_cd/da/pc/ax(CD/DA/PC/AX), lbl_wifi(WiFi), lbl_ble(BLE)
    - 新增 obj 圆点 LED (radius=3/4, 圆形):
      mute_led:    6×6  红#FF3300=静音 / 暗#0A1830=正常
      auto_led:    6×6  蓝#0090E8=自动 / 灰#4A6890=手动
      sig_cd_led:  6×6  蓝#0090E8=有信号 / 暗#0A1830=无信号
      sig_dac_led: 6×6  (同上)
      sig_pc_led:  6×6  (同上)
      sig_aux_led: 6×6  (同上)
      wifi_led:    8×8  绿#00CC66=已连接 / 暗#2A3A50=未连接
      ble_led:     8×8  蓝#0090E8=已连接 / 暗#2A3A50=未连接
    - 保留文字: lbl_input (当前输入名), lbl_temp (温度值)
    - 颜色更新: 在 50ms interval lambda 中直接调用
      lv_obj_set_style_bg_color() 确保实时刷新

  ▸ 状态栏 SVG 图标替代 LED 圆点 (迭代6)
    - 设计理念: 白色透明底 SVG 图标 + LVGL recolor 运行时着色
      优势: 真实图标 (喇叭/WiFi/蓝牙等) 比圆点更直观, 且只需白色源图
      着色机制: lv_obj_set_style_img_recolor + img_recolor_opa
    - 新建 icons/ 目录, 6 个 SVG 图标文件 (源图任意尺寸, resize 自动缩放):
      icon_mute.svg   → 显示 14×14  喇叭+X 静音图标
      icon_auto.svg   → 显示 14×14  双向切换箭头图标
      icon_signal.svg → 显示 12×12  音频信号波形条图标 (4路共用)
      icon_wifi.svg   → 显示 14×14  WiFi 信号弧图标
      icon_ble.svg    → 显示 14×14  蓝牙符号图标
      icon_thermo.svg → 显示 12×12  温度计图标
    - 新增 YAML image: 顶级组件 (6 个 img_mute/auto/signal/wifi/ble/thermo)
    - 替换控件: obj 圆点 LED → image 图标
      mute_icon:    img_mute   红#FF3300=静音 / 暗#0A1830=正常
      auto_icon:    img_auto   蓝#0090E8=自动 / 灰蓝#4A6890=手动
      sig_cd_icon:  img_signal 蓝#0090E8=有信号 / 暗#0A1830=无信号
      sig_dac_icon: img_signal (同上)
      sig_pc_icon:  img_signal (同上)
      sig_aux_icon: img_signal (同上)
      wifi_icon:    img_wifi   绿#00CC66=已连接 / 暗灰#2A3A50=未连接
      ble_icon:     img_ble    蓝#0090E8=已连接 / 暗灰#2A3A50=未连接
      thermo_icon:  img_thermo 灰#5A7A9A=常亮 (温度计, 在温度文字前)
    - lambda 更新: lv_obj_set_style_bg_color → lv_obj_set_style_img_recolor
      + lv_obj_set_style_img_recolor_opa(opa=COVER) 确保完全着色
    - 温度: 新增温度计图标在数值左侧 (thermo_icon x=288)
    - 保留文字: lbl_input (当前输入名), lbl_temp (温度数值)
    - 用户可随时替换 icons/ 目录下的 SVG/PNG 文件, 保持文件名不变
      源图任意尺寸 (如 40×40, 128×128 均可), resize 参数自动缩放到显示尺寸

  ▸ 高端 HiFi 品牌风格图标扩展 (迭代7)
    - 参考品牌: McIntosh(VU表/Power Guard), Accuphase(指针表/平衡端子),
      Mark Levinson(极简/相位), Marantz(纯直通), NAD(数字/模拟标识)
    - 新增 9 个图标文件 (icons/ 目录共 15 个):
      icon_power.svg    14×14  电源/待机 (IEC标准电源符号)
      icon_vu.svg       14×14  VU电平表 (McIntosh标志性双指针)
      icon_headphone.svg 14×14 耳机 (全品牌通用)
      icon_direct.svg   14×14  直通/纯音模式 (Marantz Pure Direct)
      icon_phase.svg    14×14  相位指示 (Mark Levinson)
      icon_clip.svg     14×14  削波/过载警告 (专业音频)
      icon_digital.svg  12×12  数字输入 (NAD/Cambridge Audio 风格)
      icon_analog.svg   12×12  模拟输入 (正弦波+A标识)
      icon_balance.svg  14×14  平衡连接/XLR (Accuphase)
    - 状态栏新增启用图标:
      input_analog_icon / input_digital_icon: 输入类型 (同位重叠)
        CD/AUX → img_analog (正弦波, 灰#5A7A9A)
        DAC/PC → img_digital (0/1, 灰#5A7A9A)
        实现方式: lv_obj_add_flag/clear_flag(LV_OBJ_FLAG_HIDDEN) 切换显隐
      power_icon: 电源状态
        工作 → img_power 绿#00CC66, 待机 → 暗橙#7A4A20
    - 预留图标 (已注册 image, 功能待扩展):
      img_vu / img_headphone / img_direct / img_phase / img_balance_conn
    - 状态栏新布局:
      [输入名] [类型] [静音] [自动] [信号×4]  ... [温度计+温度] [电源] [WiFi] [BLE]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【当前参数速查表】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  硬件平台:
    MCU:     ESP32-S3 @ 240MHz
    PSRAM:   八线 80MHz, 64KB 数据缓存, 32KB 指令缓存
    显示:    NV3007 2.79" TFT 428×142, SPI2_HOST @ 80MHz, RGB565
    音量IC:  PGA2311, SPI3_HOST @ 4MHz, CS=IO42/CLK=IO41/MOSI=IO2
    频谱IC:  MSGEQ7, RST=IO45/STB=IO39/R_OUT=IO3/L_OUT=IO9
    NTC:     IO4 (ADC1_CH3, 10kΩ+9.4kΩ@25°C 分压, B=3950)
    编码器:  IO17(A)/IO18(B)/IO14(按键)
    继电器:  IO5(IN1)/IO6(IN2)/IO7(IN3)/IO15(IN4)
    静音:    IO16 (硬件静音 GPIO)
    背光:    IO1 (LEDC PWM 5kHz)
    音频检测: IO10(CD)/IO11(DAC)/IO12(PC)/IO13(AUX)
    I2C:     SDA=IO38, SCL=IO40 @ 400kHz (预留)

  MSGEQ7 参数:
    供电:     3.3V (安全, 芯片规格 2.7~5.5V)
    STROBE:   40μs 低 + 40μs 高 (3.3V 需要更长建立时间)
    RESET:    100μs 高脉冲 + 100μs 后等待
    噪声门限: 8 (0-255 量纲, 3.3V 底噪约 8-15)
    零漂校准: setup() 3帧采样, 存于 _r_offset[7]/_l_offset[7]
    频段增益: Q8 定点 {230,256,282,320,371,435,512}
    平滑系数: 上升 0.35, 下降 0.12
    峰值衰减: 每秒保留 0.49, 帧率无关 (powf)
    NTC IIR:  α=0.15, 约 7s 时间常数 @10s 间隔
    ADC:      频谱 12dB 衰减, NTC 6dB 衰减 + curve_fitting 校准
    读取频率: 频谱 20Hz (50ms), NTC 0.1Hz (10s)

  PGA2311 参数:
    SPI 时钟: 4MHz (绝对上限 6.25MHz, 安全余量 36%)
    协议:     CS低 → 16bit (R8+L8) → CS高
    数值:     0=静音, 1=-95.5dB, 192≈0dB, 255=+31.5dB
    Fade:     双区间渐变 (>10dB:0.12, >3dB:0.08, ≤3dB:0.18), 15ms 步进

  LVGL 参数:
    缓冲区:   100% 全帧 (~121KB, PSRAM)
    颜色深度: 16bit RGB565
    刷新:     仅脏区, partial_refresh
    渲染加速: SIMD + FPU

  UI 布局 (主页面 428×142):
    状态栏:   y=0~15 (输入源+类型图标/静音/自动/信号×4/温度计+温度/电源/WiFi/BLE)
    分隔线:   y=15, h=1
    音量条:   x=4, y=18, w=16, h=100
    音量标签: x=24, y=22 (百分比/dB/平衡)
    频谱条:   x=68起, y=18, w=46×7, gap=6, h=100
    峰值线:   跟随频谱 x, h=2, 白色 opa=180
    频率标签: y=120, 居中对齐 (63/160/400/1k/2.5k/6k/16k)

  UI 布局 (设置页 428×142):
    三列 flex 布局
    左列: 最大音量 (10~255)
    中列: 声道平衡 (-20~20)
    右列: 输入选择 (CD/DAC/PC/AUX) + 屏幕亮度 (0~100)

  按键手势:
    单击 (<500ms): 唤醒屏幕 或 LVGL encoder enter
    双击 (<400ms×2): 切换主页面/设置页
    长按 (≥800ms): 待机切换

  自动输入切换:
    开关: auto_input (默认开启, HA auto_input_switch 可控)
    优先级: CD > DAC > PC > AUX
    触发: 音频传感器 on_press/on_release → auto_switch_input 脚本
    防抖: 500ms (mode:restart, 等所有传感器稳定)
    状态栏图标: mute_icon(喇叭)/auto_icon(切换)/sig_cd~aux_icon(波形)/wifi_icon(WiFi)/ble_icon(蓝牙)/thermo_icon(温度计)
    图标着色: 亮色=有效(蓝/绿/红), 暗色=无效(#0A1830/#2A3A50)
    图标文件: icons/ 目录下 6 个白色透明底 SVG, 用户可替换

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【已知注意事项】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  1. LVGL 对象指针访问
     代码中使用 id(spec_0)->obj_ 访问 LVGL 对象
     如果编译失败, 可能需要改为 id(spec_0)->lv_obj 或其他成员名
     (取决于 ESPHome 版本)

  2. PSRAM 配置 — 强绑定八线 (Octal) PSRAM
     a) Octal 模式绑定 (CONFIG_SPIRAM_MODE_OCT)
        此固件的 sdkconfig 强制开启八线 PSRAM 模式。
        如果烧录到 Quad PSRAM（四线）的廉价 ESP32-S3 开发板上，
        PSRAM 初始化失败，将导致无限 Bootloop（启动死机）。
        移植到其他 S3 开发板前，务必确认 PSRAM 类型并调整此选项。

     b) 指令/只读数据取自 PSRAM (CONFIG_SPIRAM_FETCH_INSTRUCTIONS / CONFIG_SPIRAM_RODATA)
        要求固件大小不超过 PSRAM 可用空间，否则启动失败。
        如遇启动问题，先关闭这两个选项排查。

  3. MSGEQ7 零漂校准前提
     假设启动时无音频输入, 如果开机时有信号, 偏置值会偏高
     导致正常播放时频谱偏低 — 可通过 ESP_LOGI 输出验证

  4. NTC 温度范围限制
     使用 6dB 衰减 (0~2.5V), 0°C 时 NTC≈31kΩ → V≈2.5V
     低于 0°C 可能削顶, 室内前级场景可接受

  5. PGA2311 SPI 绝对不可超频
     fSCLK 最高 6.25MHz, 超频可导致数据错位 → 瞬间满输出
     这不是性能问题, 是安全隐患 (可烧毁喇叭/损伤听力)
     当前 4MHz 是安全值, 如需提速也不应超过 6MHz

  6. SPI 总线分离
     显示 SPI2_HOST (DMA, 80MHz) 与 PGA2311 SPI3_HOST (4MHz)
     两者独立, 无总线共享冲突

  7. lvgl_compat.h extern "C" 版本依赖
     lvgl_compat.h 使用 extern "C" 绕过 managed_component 的 opaque 限制，
     直接声明 LVGL C API 函数。这种做法深度依赖当前 LVGL 版本（v9.x）的函数签名。
     升级 ESPHome 大版本（如 2026.x → 2027.x）时，如果底层 LVGL 调整了函数签名
     （如添加 const 限定符或新增缺省参数），将在链接阶段报错（Linker Error）。
     修复方法：删除 extern "C" 块，改用标准 #include <lvgl.h> 方式。
     这是编译期安全失败，不会导致运行时异常。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【升级检查清单 — 每次修改前必读】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  □ PGA2311 SPI 时速 ≤ 6.25MHz? (当前 4MHz ✓)
  □ MSGEQ7 STROBE 延时 ≥ 36μs? (当前 40μs ✓)
  □ NOISE_GATE 匹配供电电压? (3.3V → 8 ✓)
  □ LVGL buffer 在 PSRAM 可容纳? (121KB < 8MB ✓)
  □ sdkconfig 选项与 ESP-IDF 版本兼容?
  □ LVGL obj_ 成员名与 ESPHome 版本匹配?
  □ 零漂校准日志正常? (启动时查看 _r_offset/_l_offset)
  □ NTC 温度显示稳定? (IIR 平滑后无闪烁)
  □ 自动切换防抖合理? (当前 500ms, 传感器已有 100ms/500ms 滤波)
  □ auto_input 状态栏图标着色正确? (蓝=自动, 灰蓝=手动)
  □ SVG 图标文件在 icons/ 目录? (6个文件, ESPHome编译时读取)
  □ 图标 recolor 机制正常? (lv_obj_set_style_img_recolor + opa=COVER)
  □ lvgl_compat.h extern "C" 与当前 LVGL 版本兼容? (升级后如遇链接错误, 删除 extern "C" 块改用标准头文件)
